# AgentScope 多智能体 AI 问答系统 — 功能设计文档

> 基于代码结构梳理，版本日期：2026-07-14

---

## 一、系统概述

本系统是一个基于 **FastAPI + AgentScope SDK** 构建的多智能体 AI 问答平台，核心能力包括：

- **多智能体编排**：支持并行（Parallel）、流水线（Pipeline）、ReAct 动态推理三种编排模式
- **多意图识别**：LLM 驱动的意图识别引擎，单次请求可拆解为多个子意图并分派给不同智能体
- **查询改写**：结合上下文消解指代、补全省略、替换时间代词
- **Docker 工作区隔离**：按用户分配独立 Docker 容器，实现文件系统与技能装载隔离
- **管理中心集成**：对接 MNG 系统实现登录注册、权限控制、外部意图动态注入
- **可视化组件渲染**：折线图/柱状图/饼图/指标卡/可选列表/确认操作等前端组件
- **可观测性**：Langfuse 追踪集成，支持对话评分反馈

### 技术栈

| 层级 | 技术 |
|------|------|
| Web 框架 | FastAPI + Uvicorn |
| AI SDK | AgentScope ≥ 2.0.0 |
| LLM | OpenAI 兼容协议（DeepSeek 等） |
| 数据库 | MySQL 8.0（aiomysql 异步驱动） |
| 缓存 | Redis 7（aioredis 异步驱动） |
| 容器化 | Docker + docker-compose |
| 加密 | 国密 SM4-CBC（gmssl） |
| 可观测性 | Langfuse ≥ 3.5.0 |
| 定时任务 | APScheduler |

### 端口与服务

| 服务 | 端口 |
|------|------|
| 应用服务 | 7010 |
| MySQL | 3306 |
| Redis | 6379 |

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                       前端（Web Client）                      │
└────────────────────────────┬────────────────────────────────┘
                             │ HTTP/SSE
┌────────────────────────────▼────────────────────────────────┐
│                    FastAPI 路由层（Routes）                   │
│  auth / chat / feedback / files / health / sessions /       │
│  upload / mng_proxy / action_audit                          │
└────────────────────────────┬────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────┐
│                    服务层（Services）                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ ChatService  │  │ AuthService  │  │ Orchestrator │      │
│  │ (对话适配)    │  │ (JWT鉴权)    │  │ Service(编排) │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ SessionSvc   │  │ FileService  │  │ MngService   │      │
│  │ (会话管理)    │  │ (文件处理)    │  │ (管理中心)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ LangfuseSvc  │  │ WorkspaceMgr│  │ CleanupSvc   │      │
│  │ (可观测性)    │  │ (Docker工作区)│  │ (定时清理)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────────────┬────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────┐
│              智能体/意图/编排层（Agents/Intent/Orchestrator）  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ AgentRegistry│  │ AgentFactory │  │ IntentRecogn.│      │
│  │ (智能体注册)  │  │ (智能体工厂)  │  │ (意图识别)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ QueryRewriter│  │ Parallel Orch│  │ Pipeline Orch│      │
│  │ (查询改写)    │  │ (并行编排)    │  │ (流水线编排)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐                                           │
│  │ ReAct Orch   │                                           │
│  │ (动态推理编排)│                                           │
│  └──────────────┘                                           │
└────────────────────────────┬────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────┐
│                 数据访问层（DAO）+ 基础设施                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ SessionDAO   │  │ UserDAO      │  │ ActionAudit  │      │
│  │ (MySQL)      │  │ (MNG/模拟)   │  │ DAO(MySQL)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐                           │
│  │ MySQL 8.0    │  │ Redis 7      │                           │
│  └──────────────┘  └──────────────┘                           │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 请求处理主流程（/chat）

```
用户请求 → JWT鉴权 → Session获取/创建
    │
    ├─ 加载会话历史 → 拼接完整消息列表
    ├─ 快照工作目录（检测文件变更用）
    │
    ▼
OrchestratorService.run()
    │
    ├─ 1. 动态加载配置（YAML + Redis缓存/兜底）
    │     ├─ 读取登录时缓存的融合配置（意图+智能体+技能）
    │     └─ 缓存未命中 → base-only 兜底
    │
    ├─ 2. Docker工作区获取/创建
    │
    ├─ 3. 构建临时组件（Registry + Factory + Rewriter + Recognizer）
    │
    ├─ 若指定 agent_id → 单智能体直接问答
    │
    ├─ 4. 查询改写（QueryRewriter）
    │     └─ 结合历史上下文 + 时间代词替换
    │
    ├─ 5. 意图识别（IntentRecognizer）
    │     └─ LLM 单次调用 → 结构化 JSON → 多意图 + 关系
    │
    ├─ 6. 编排模式选择（relation → mode）
    │     ├─ independent → Parallel
    │     ├─ related_fixed → Pipeline
    │     └─ related_dynamic → ReAct
    │
    ├─ 7. 执行编排（yield SSE事件流）
    │
    └─ 8. 后处理
          ├─ 持久化对话历史（MySQL messages表）
          ├─ 持久化 AgentState（MySQL agent_states表）
          ├─ 检测新文件 → files_generated 事件
          ├─ 生成推荐问题 → recommended_questions 事件
          ├─ Langfuse 追踪 → trace_ready 事件
          └─ 保存 trace_id 到会话元信息
```

---

## 三、功能模块详细设计

### 3.1 认证与鉴权模块（Auth）

#### 3.1.1 登录（POST /login）

**功能描述**：验证用户凭据，签发 JWT + Refresh Token，缓存用户权限与融合配置到 Redis。

**处理流程**：
1. 调用 `verify_login` 校验用户名密码
   - `AUTH_MOCK=true`：使用内置模拟账号（zhangsan/admin）
   - `AUTH_MOCK=false`：调用 MNG 管理中心 `POST {MNG_AUTH_URL}/api/auth/user/login`
2. 签发 Access Token（有效期 JWT_EXPIRE_HOURS，默认8小时）和 Refresh Token（有效期7天）
3. 将用户权限（agent_whitelist + skill_blacklist）存入 Redis（TTL 与 JWT 一致）
4. 融合用户配置（YAML基础配置 + MNG外部意图 + 权限过滤）并缓存到 Redis
5. 异步上报 MNG 用户活跃（fire-and-forget）

**返回数据**：
```json
{
  "code": 200,
  "data": {
    "verification": true,
    "token": "<access_token>",
    "token_type": "bearer",
    "expires_in": 28800,
    "refresh_token": "<refresh_token>",
    "refresh_expires_in": 604800,
    "user_info": {"id", "username", "name", "department", "role"},
    "agent_access": [...],
    "skill_blacklist": [...]
  }
}
```

#### 3.1.2 注册（POST /register）

**功能描述**：注册新用户，流程与登录一致（成功后直接签发 JWT 并缓存配置）。

- Mock 模式：直接返回新用户结构（空权限）
- MNG 模式：调用 `POST {MNG_AUTH_URL}/api/auth/register`

#### 3.1.3 Token 刷新（POST /refresh）

**功能描述**：凭 Refresh Token 换取新的 Access Token。固定有效期，不滚动刷新。需验证 Redis 中用户登录态是否仍有效。

#### 3.1.4 用户信息更新

| 接口 | 方法 | 功能 |
|------|------|------|
| /api/auth/me/name | PUT | 修改姓名（调MNG → 重签JWT） |
| /api/auth/me/department | PUT | 修改部门（调MNG → 重签JWT） |
| /api/auth/me/password | PUT | 修改密码（调MNG → 重签JWT） |

所有更新操作成功后重签 JWT 并返回与登录一致的响应结构。

#### 3.1.5 JWT 鉴权机制

- 算法：HS256
- Payload：`{user_id, username, name, department, role, iat, exp}`
- 依赖注入：`current_user` 从 `Authorization: Bearer <token>` 解析
- Refresh Token：额外包含 `type: "refresh"` 字段

#### 3.1.6 敏感配置加密

采用国密 SM4-CBC 对称加密：
- 密文格式：`ENC(<base64(iv + ciphertext)>)`
- 主密钥：`CONFIG_DECRYPT_KEY`（32位hex → 16字节）
- 支持 `JWT_SECRET`、`MYSQL_PASSWORD` 等敏感配置加密存储
- 明文模式向后兼容（不带 `ENC(...)` 前缀视为明文）

---

### 3.2 对话模块（Chat）

#### 3.2.1 聊天接口（POST /chat）

**功能描述**：核心对话接口，流式返回 SSE 事件。

**请求参数**：
```json
{
  "messages": [{"role": "user", "content": "..."}],
  "session_id": "可选，已有会话ID",
  "agent_id": "可选，指定智能体ID跳过编排",
  "search_enabled": true
}
```

**SSE 事件流序列**：

| 事件类型 | 说明 |
|----------|------|
| `session_ready` | 返回 session_id |
| `query_rewritten` | 查询改写结果 |
| `intents_recognized` | 意图识别结果（intents + relation） |
| `orchestration_start` | 编排开始（mode + intent_count） |
| `task_start` | 单个任务启动 |
| `AgentEvent` | AgentScope 原生事件（文本流、工具调用等） |
| `CUSTOM_COMPONENT` | 可视化组件事件（从 TOOL_RESULT_TEXT_DELTA 中提取） |
| `task_end` | 单个任务完成 |
| `summary` | 最终汇总输出 |
| `files_generated` | 本轮新生成的文件列表 |
| `recommended_questions` | 推荐的3个后续问题 |
| `trace_ready` | Langfuse trace_id |

#### 3.2.2 推荐问题生成

对话结束后，调用 LLM 根据本轮问答生成 3 个推荐后续问题，任何异常静默降级（不影响主流程）。

#### 3.2.3 自定义组件检测

从 `TOOL_RESULT_TEXT_DELTA` 事件中提取 `type` 为 `chart`/`volume_chart`/`selectable_list`/`confirm_action` 的 JSON 对象，转换为 `CUSTOM_COMPONENT` 事件推送给前端。

---

### 3.3 意图识别模块（Intent）

#### 3.3.1 查询改写器（QueryRewriter）

**功能描述**：将口语化、省略、指代性输入改写为语义完整的规范查询。

**改写策略**：
- 无历史上下文且不含时间代词 → 跳过改写（避免每次首条消息都调 LLM）
- 含时间代词（今天/昨天/本周/本月/今年/最近/近期等）→ 必须改写
- 注入服务器当前时间（Asia/Shanghai）到 system prompt
- 最近6轮对话作为上下文
- 改写失败 → 原样返回（降级）

#### 3.3.2 意图识别器（IntentRecognizer）

**功能描述**：LLM 单次调用输出结构化 JSON，识别多意图及关系。

**识别流程**：
1. 拼接识别 prompt（含可识别意图清单 + 历史上下文 + 用户输入）
2. LLM 调用，输出 JSON：`{intents: [{id, query}], relation}`
3. 解析结果，回填 intent → agent 映射
4. 未知意图 id → 降级为 `general_chat`
5. 识别失败 → 兜底返回 single `general_chat`

**意图间关系**：

| relation | 含义 | 编排模式 |
|----------|------|----------|
| `independent` | 各意图互不依赖 | Parallel |
| `related_fixed` | 固定先后依赖 | Pipeline |
| `related_dynamic` | 需动态决策 | ReAct |

**配置的意图**：

| 意图 ID | 名称 | 绑定智能体 |
|---------|------|-----------|
| render_chart | 数据可视化 | chart_agent |
| general_chat | 通用问答 | general_agent |
| regulations_qa | 制度问答 | regulations_agent |

---

### 3.4 智能体模块（Agents）

#### 3.4.1 智能体定义（AgentDefinition）

每个智能体包含：`id`、`name`、`skills`（绑定技能列表）、`system_prompt`

**预配置智能体**：

| 智能体 ID | 名称 | 绑定技能 |
|-----------|------|----------|
| chart_agent | 数据可视化智能体 | chart_renderer, card_interaction |
| general_agent | 通用问答智能体 | bocha_search |
| regulations_qa | 制度问答智能体 | ragflow_retrieval |

#### 3.4.2 智能体注册表（AgentRegistry）

**职责**：
- 管理所有智能体定义
- 按智能体绑定的 skill 子集筛选工具，组装独立 Toolkit（带缓存）
- 创建 Agent 实例（每次新建模型实例，流式模型不可复用）
- 无 skill 的智能体（如 general_agent）返回空 Toolkit（纯对话无工具）

#### 3.4.3 智能体工厂（AgentFactory）

**职责**：意图 → 智能体实例的门面，封装 AgentRegistry 的创建逻辑。

- `create_for_agent(agent_id)` → 根据 agent_id 创建智能体
- `create_fallback()` → 降级兜底，强制走 general_agent

---

### 3.5 编排器模块（Orchestrator）

#### 3.5.1 并行编排器（ParallelOrchestrator）

**适用场景**：多意图无关联，可独立并行执行。

**执行流程**：
1. `asyncio.gather` 并行执行所有意图对应智能体
2. 每个智能体有独立超时（parallel_timeout，默认60秒）
3. 收集所有结果，回放 SSE 事件
4. 多结果用分隔线 `---` 汇总

#### 3.5.2 流水线编排器（PipelineOrchestrator）

**适用场景**：多意图有关联，顺序固定。

**执行流程**：
1. 按意图列表固定顺序串行执行
2. 每步输出作为下一步的 `prior_context`（前置参考信息）
3. 任一步失败 → 终止流水线（发送 `pipeline_intercept` 事件）
4. 每步有独立超时（pipeline_step_timeout，默认60秒）

#### 3.5.3 ReAct 编排器（ReActOrchestrator）

**适用场景**：多意图有关联，需根据中间结果动态决策。

**执行流程**：
1. 初始化推理链 `scratch = ""`
2. 循环（最多 MAX_STEPS=8 次）：
   - **Thought**：LLM 根据 scratch 决定下一步（is_final? next_action?）
   - 若 `is_final` → 输出结论，结束
   - **Act**：执行选定智能体/动作
   - **Observe**：收集结果，追加到 scratch
3. 步数超限 → 兜底输出

---

### 3.6 编排服务（OrchestratorService）

**核心职责**：串起 查询改写 → 意图识别 → 编排器选择 → 编排执行 的完整主流程。

**关键设计**：
- **动态配置加载**：每次 `run()` 请求时动态加载 YAML + Redis 缓存配置，请求结束后内存自动释放
- **配置融合**：登录时融合（YAML基础 + MNG外部意图 + 权限过滤）并缓存到 Redis，会话时直接读取
- **缓存兜底**：Redis 缓存未命中 → base-only 兜底（不请求 MNG，无外部意图）
- **单智能体直答**：传入 `agent_id` 时跳过改写/识别/编排，直接执行指定智能体
- **联网搜索开关**：`search_enabled` 控制是否启用 bocha_search 技能

---

### 3.7 会话管理模块（Sessions）

#### 3.7.1 会话列表（GET /sessions）

返回当前用户的会话列表，分为置顶会话（top_sessions，最多5条）和普通会话（sessions，按更新时间降序，最多15条）。

#### 3.7.2 会话详情（GET /sessions/{session_id}）

返回会话详情，包含：对话历史（messages）+ 历史生成文件（files）+ trace_id。校验会话归属（越权返回403）。

#### 3.7.3 置顶/取消置顶（PUT /sessions/{session_id}/pin）

#### 3.7.4 删除会话（DELETE /sessions/{session_id}）

CASCADE 自动清理关联的 agent_states、messages、session_files。

---

### 3.8 文件管理模块

#### 3.8.1 文件上传（POST /upload）

**功能描述**：上传文件到会话工作目录。

- 文件大小校验（默认最大10MB）
- 文件类型校验（支持：JPEG/PNG/GIF/WebP/PDF/TXT/DOCX/CSV/XLS/XLSX/PPT/PPTX）
- 若未提供 session_id → 自动创建新会话
- 文件保存到 `{WORKSPACE_BASEDIR}/{session_id}/data/` 目录
- 文件名加 UUID 前缀避免冲突

#### 3.8.2 文件下载（GET /files/{session_id}/{path}）

**功能描述**：下载指定会话工作目录下的文件。

- 越权校验：realpath 后必须仍在 `{WORKSPACE_BASEDIR}/{session_id}` 内
- 自动推断 MIME 类型

#### 3.8.3 文件变更检测

每轮对话开始前快照工作目录，结束后 diff 检测新增文件，生成 `files_generated` 事件推送给前端，并持久化到 `session_files` 表。

---

### 3.9 Docker 工作区管理模块

#### 3.9.1 工作区管理器（DockerWorkspaceManager）

**核心设计**：
- **按用户隔离**：同一 user_id 复用同一 Docker 容器，不同 user_id 各自独立
- **会话路径隔离**：容器挂载 basedir 根目录，工作路径按 session_id 隔离（`ws.workdir = {basedir}/{session_id}`）
- **TTL 淘汰**：空闲超 TTL（默认3600秒）的 workspace 被后台 sweeper 惰性/周期淘汰并销毁
- **技能装载**：创建容器时将技能目录挂载进去

#### 3.9.2 工作区清理服务（WorkspaceCleanupService）

**功能描述**：定时扫描 WORKSPACE_BASEDIR，删除最后修改时间超过 retention_days（默认7天）的目录。清理间隔可配置（默认24小时）。

---

### 3.10 管理中心集成模块（MNG）

#### 3.10.1 外部意图获取（fetch_external_intents）

调用 `GET {MNG_INTENT_URL}/api/intents`，获取 MNG 管理中心配置的外部意图列表。失败降级为空列表（不影响主流程）。

#### 3.10.2 配置融合（merge_external_into_memory）

**融合规则**：
1. 加载 YAML 基础配置（agents + intents + skills）
2. 获取 MNG 外部意图列表
3. **权限过滤**：
   - 外部 agent 不在 `agent_whitelist` 中 → 跳过
   - 外部 skill 在 `skill_blacklist` 中 → 跳过
4. 合并：基础配置 + 过滤后的外部配置
5. 外部 skill 目录映射：`{EXTERNAL_SKILLS_DIR}/{skill.code}`

#### 3.10.3 MNG 代理接口

| 接口 | 功能 |
|------|------|
| GET /api/presentation/cards | 代理获取卡片配置 |
| GET /api/presentation/custom-components | 代理获取自定义组件配置 |

请求头中转发本系统 JWT 作为 Authorization。

#### 3.10.4 用户活跃上报

登录和每次 /chat 时异步上报 MNG 用户活跃（fire-and-forget，失败不影响主流程）。

---

### 3.11 反馈模块（Feedback）

#### POST /feedback

**功能描述**：提交用户对某次对话的评分反馈。

- 依赖 Langfuse 服务（未启用返回503）
- 调用 Langfuse `create_score` API，将 liked（布尔值）转换为 1.0/0.0 分数
- 支持 trace_id 关联 + 文字评论

---

### 3.12 审计模块（ActionAudit）

#### POST /action-audit

**功能描述**：记录动作确认审计日志。

- 记录字段：userId、action、query、confirm（布尔值）
- 持久化到 MySQL `action_audit` 表

---

### 3.13 健康检查模块

| 接口 | 功能 |
|------|------|
| GET /health | 返回服务状态 + 已加载智能体数量 |
| GET /agents | 列出所有已注册智能体及其绑定技能 |

---

### 3.14 可观测性模块（Langfuse）

**功能描述**：LLM 对话追踪与评分。

- 非强依赖：未配置密钥时自动禁用
- 每次对话创建 observation（span），记录输入输出
- 对话结束后 flush 并返回 trace_id
- 支持用户反馈评分（create_score）

---

## 四、技能（Skills）体系

### 4.1 技能配置

技能通过 `skill_config.yml` 声明，运行时通过 Docker 工作区装载。每个智能体按 `agent_config.yml` 中的 `skills` 字段绑定技能子集。

### 4.2 内置技能

| 技能名 | 目录 | 描述 |
|--------|------|------|
| bocha_search | skills/bocha_search | 博查互联网搜索（curl 调用 Bocha API） |
| chart_renderer | skills/chart_renderer | 智能图表渲染（折线/柱状/饼图/指标表/指标卡） |
| card_interaction | skills/card_interaction | 个性化交互卡片（通用卡片/可选列表/确认操作） |
| ragflow_retrieval | skills/ragflow_retrieval | RAGFlow 知识库检索（制度问答） |

### 4.3 内置工具（Tools）

系统级工具在编排服务中直接注册，所有智能体共享：

| 工具 | 来源 | 说明 |
|------|------|------|
| Bash/Read/Write/Edit/Glob/Grep | AgentScope SDK | 文件系统操作工具 |
| render_line_chart | tools/chart_tools | 折线图渲染 |
| render_bar_chart | tools/chart_tools | 柱状图渲染 |
| render_pie_chart | tools/chart_tools | 饼图渲染 |
| render_indicator_table | tools/chart_tools | 技术指标表格渲染 |
| render_metric_card | tools/chart_tools | 核心指标卡渲染 |
| render_generic_card | tools/chart_tools | 通用卡片渲染 |
| render_selectable_list | tools/chart_tools | 可选列表渲染 |
| render_confirm_action | tools/chart_tools | 确认操作渲染 |

**工具输出格式**：所有渲染工具返回标准化 component JSON（`ToolResponse`），由 `chat_service` 拦截 `TOOL_RESULT_TEXT_DELTA` 事件，提取 component 并转换为 `CUSTOM_COMPONENT` SSE 事件推送给前端。

---

## 五、数据模型

### 5.1 MySQL 表结构

#### sessions（会话元信息）

| 字段 | 类型 | 说明 |
|------|------|------|
| session_id | VARCHAR(64) PK | 会话ID |
| user_id | VARCHAR(64) | 用户ID |
| name | VARCHAR(255) | 会话名称（首条用户消息截取50字） |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |
| message_count | INT | 消息数量 |
| latest_trace_id | VARCHAR(255) | 最新 Langfuse trace ID |
| is_pinned | TINYINT(1) | 是否置顶 |
| pinned_at | TIMESTAMP | 置顶时间 |
| agent_ids | JSON | 参与的智能体ID列表 |

#### agent_states（智能体状态）

| 字段 | 类型 | 说明 |
|------|------|------|
| session_id | VARCHAR(64) | 会话ID（FK → sessions） |
| agent_id | VARCHAR(64) | 智能体ID |
| state | JSON | AgentState 完整序列化 |
| updated_at | TIMESTAMP | 更新时间 |

联合主键：(session_id, agent_id)，CASCADE 删除。

#### messages（对话消息）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 消息ID |
| session_id | VARCHAR(64) | 会话ID（FK → sessions） |
| role | VARCHAR(32) | 角色（user/assistant） |
| content | TEXT | 消息内容 |
| timestamp | TIMESTAMP | 消息时间 |
| agent_ids | JSON | 参与的智能体ID列表 |

CASCADE 删除。

#### session_files（会话生成文件）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 文件ID |
| session_id | VARCHAR(64) | 会话ID（FK → sessions） |
| name | VARCHAR(255) | 文件名 |
| path | VARCHAR(512) | 相对路径 |
| url | VARCHAR(512) | 下载URL |
| size | BIGINT | 文件大小（字节） |
| media_type | VARCHAR(255) | MIME类型 |

唯一约束：(session_id, path)，CASCADE 删除。

#### action_audit（动作审计）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT PK | 记录ID |
| userId | VARCHAR(64) | 用户ID |
| action | VARCHAR(128) | 动作类型 |
| query | VARCHAR(1024) | 查询内容 |
| confirm | TINYINT(1) | 是否确认 |
| created_at | TIMESTAMP | 创建时间 |

### 5.2 Redis Key 设计

| Key 模式 | TTL | 说明 |
|----------|-----|------|
| `user_permissions:{user_id}` | JWT_EXPIRE_HOURS * 3600 | 用户权限数据 |
| `user_config:{user_id}` | JWT_EXPIRE_HOURS * 3600 | 融合后的用户配置（意图+智能体+技能） |

---

## 六、配置体系

### 6.1 YAML 配置文件

| 文件 | 说明 |
|------|------|
| config/agent_config.yml | 智能体定义（id/name/skills/system_prompt） |
| config/intent_config.yml | 意图定义 + 编排策略 + 编排器参数 |
| config/model_config.yml | 模型配置 + Prompt 模板 |
| config/skill_config.yml | 技能声明（name/directory/description） |

### 6.2 环境变量配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| CONFIG_DECRYPT_KEY | 空 | SM4 主密钥（32位hex） |
| JWT_SECRET | - | JWT 签名密钥（支持ENC密文） |
| JWT_EXPIRE_HOURS | 8 | Access Token 有效期 |
| JWT_REFRESH_EXPIRE_DAYS | 7 | Refresh Token 有效期 |
| AUTH_MOCK | true | 是否使用模拟登录 |
| AUTH_API_URL | - | MNG 登录校验接口 |
| MNG_AUTH_URL | - | MNG 用户鉴权地址 |
| MNG_INTENT_URL | - | MNG 意图/卡片地址 |
| REDIS_URL | redis://localhost:6379/0 | Redis 连接 |
| MYSQL_HOST/PORT/USER/PASSWORD/DATABASE | - | MySQL 连接 |
| UPLOAD_MAX_SIZE_MB | 10 | 文件上传大小限制 |
| OPENAI_API_KEY | - | LLM API Key |
| LANGFUSE_PUBLIC_KEY/SECRET_KEY/HOST | - | Langfuse 可观测性 |
| WORKSPACE_BASE_IMAGE | python:3.13-slim | Docker 工作区基础镜像 |
| WORKSPACE_BASEDIR | /data/docker-workspaces | 工作区根目录 |
| WORKSPACE_TTL | 3600 | 工作区空闲超时（秒） |
| WORKSPACE_RETENTION_DAYS | 7 | 工作区文件保留天数 |
| EXTERNAL_SKILLS_DIR | - | 外部技能目录 |
| RAGFLOW_API_KEY/BASE_URL | - | RAGFlow 知识库配置 |

---

## 七、权限与安全设计

### 7.1 权限模型

- **agent_whitelist**：用户可访问的智能体白名单（ID列表）
- **skill_blacklist**：用户被禁用的技能黑名单（code列表）

### 7.2 权限应用流程

1. 登录时从 MNG 获取用户权限
2. 权限存入 Redis，TTL 与 JWT 一致
3. 配置融合时：
   - 外部 agent 不在白名单 → 跳过
   - 外部 skill 在黑名单 → 跳过
4. 会话时按请求开关控制联网搜索技能的显隐

### 7.3 安全措施

- JWT Token 鉴权（所有业务接口需 Bearer Token）
- 文件下载越权校验（realpath 边界检查）
- 敏感配置 SM4 加密存储
- Docker 容器工作区文件系统隔离
- Agent 工作目录限制（system_prompt 中注入目录约束）

---

## 八、生命周期管理

### 8.1 应用启动（lifespan）

1. 加载模型配置
2. 初始化 Docker 工作区管理器（启动 sweeper）
3. 启动工作区定时清理服务
4. 初始化编排服务（加载 LLM 客户端 + Prompt 模板）
5. 初始化 Redis 连接
6. 初始化 MySQL 连接池 + 自动建表
7. 初始化 Langfuse 追踪服务（非强依赖）

### 8.2 应用关闭

1. 停止工作区 sweeper + 销毁所有容器
2. 停止定时清理服务
3. 关闭 MySQL 连接池
4. 关闭 Redis 连接

---

## 九、目录结构总览

```
workspace/
├── app/                          # 应用主包
│   ├── main.py                   # FastAPI 入口 + lifespan
│   ├── config.py                 # 配置加载（环境变量 + 解密）
│   ├── dependencies.py           # JWT 鉴权依赖
│   ├── agents/                   # 智能体模块
│   │   ├── base.py               # AgentDefinition 数据模型
│   │   ├── factory.py            # AgentFactory 工厂
│   │   └── registry.py           # AgentRegistry 注册表
│   ├── intent/                   # 意图识别模块
│   │   ├── models.py             # Intent/IntentResult/IntentConfig
│   │   ├── recognizer.py         # IntentRecognizer 识别器
│   │   ├── rewriter.py           # QueryRewriter 改写器
│   │   └── llm_client.py         # LLM 非流式调用客户端
│   ├── orchestrator/             # 编排器模块
│   │   ├── base.py               # BaseOrchestrator + TaskResult
│   │   ├── parallel.py           # ParallelOrchestrator
│   │   ├── pipeline.py           # PipelineOrchestrator
│   │   └── react.py              # ReActOrchestrator
│   ├── routes/                   # API 路由
│   │   ├── auth.py / chat.py / feedback.py / files.py
│   │   ├── health.py / sessions.py / upload.py
│   │   ├── mng_proxy.py / action_audit.py
│   ├── services/                 # 业务服务
│   │   ├── auth_service.py / chat_service.py / session_service.py
│   │   ├── file_service.py / mng_service.py / langfuse_service.py
│   │   ├── orchestrator_service.py / workspace_manager.py
│   │   ├── workspace_cleanup_service.py / action_audit_service.py
│   │   └── file_change_detector.py
│   ├── dao/                      # 数据访问层
│   │   ├── mysql_session_dao.py / user_dao.py / action_audit_dao.py
│   │   └── init_mysql.py
│   ├── models/                   # Pydantic 数据模型
│   │   ├── auth.py / chat.py / session.py / upload.py
│   │   ├── feedback.py / action_audit.py
│   └── utils/
│       └── secret_codec.py       # SM4 加解密工具
├── config/                       # YAML 配置文件
│   ├── agent_config.yml / intent_config.yml
│   ├── model_config.yml / skill_config.yml
├── skills/                       # 技能目录
│   ├── bocha_search/ / chart_renderer/
│   ├── card_interaction/ / ragflow_retrieval/
├── tools/                        # 系统级工具
│   ├── chart_tools.py / tool_constants.py / card_config_tools.py
├── scripts/
│   └── encrypt_secret.py         # 配置加密脚本
├── docker-compose.yml            # Docker Compose 编排
├── Dockerfile                    # 多阶段构建
└── requirements.txt              # Python 依赖
```
