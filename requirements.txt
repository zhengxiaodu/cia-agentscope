# AgentScope AI 问答系统依赖
agentscope>=2.0.0
fastapi>=0.100.0
uvicorn>=0.31.1
pydantic>=2.11.7
aiohttp>=3.9.0
pyyaml>=6.0.0
python-dotenv>=1.0.0
apscheduler>=3.10.0
redis>=5.0.0
PyJWT>=2.8.0
aiomysql>=0.2.0

# 国密 SM4 加密（敏感配置解密）
gmssl>=3.2.2

# LLM 可观测性
langfuse>=4.14,<5

httpx>=0.27.0

# OpenSandbox SDK（K8s 沙箱集群，工作区后端）
opensandbox>=0.1.13
opensandbox-code-interpreter>=0.1.0

# Markdown 导出工具（md_to_docx / md_to_pdf / md_to_xlsx / md_to_png）
md-exporter>=4.0.0

# 测试
pytest>=8.0
pytest-asyncio>=0.23