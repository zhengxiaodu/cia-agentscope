# 链路追踪与埋点优化 P1 后端实施计划

**目标：** 把最贵的一层（LLM 调用、工具调用）从黑盒变为 Langfuse 可下钻，并补齐 ReAct 每轮、降级原因、TTFT、pipeline 编排级埋点。

**架构：** 复用 P0 建的 `LangfuseService.start_observation/end_observation`（已支持 model/usage\_details/level）与 `get_current_langfuse()` 单例。`chat_complete()` 内建 generation；新增 `AgentEventTracer` 旁路翻译 AgentScope 事件流，经新增 helper `iter_agent_events` 接入两个 reply\_stream 循环；ReAct/pipeline 各加编排级 span；降级点改写 span output。所有观测代码异常全兜底、langfuse 未启用时短路。

**技术栈：** Python 3.12 / FastAPI / Langfuse 4.14.1 / AgentScope 2.0.5 / pytest + pytest-asyncio

## 全局约束

*   **严禁自动提交 git**：所有 git 提交由用户手动执行。本计划的"提交检查点"步骤仅列改动清单供审查，**不含任何** `**git add**` **/** `**git commit**` **命令**。
    
*   **硬前提：P0 必须先落地**。P1 依赖以下 P0 产物，实施前用 `grep` 确认存在，缺失则先执行 P0 后端计划：
    
    *   `app/services/langfuse_service.py` 的 `get_current_langfuse()` / `set_current_langfuse()`
        
    *   `start_observation(name, as_type, input, model=, metadata=)` 扩展签名
        
    *   `end_observation(observation, output, usage_details=, level=, status_message=)` 扩展签名
        
    *   `app/utils/trace_names.py` 的 `TraceName`（StrEnum）
        
    *   `app/utils/traced.py` 的 `traced_stage` / `StageRecorder`
        
    *   `tests/conftest.py`（pytest-asyncio 自动标记）
        
*   **行号会漂移**：P0 落地会改动 `chat_service.py`、`langfuse_service.py` 等文件行号。**本计划一律按函数名/符号锚点定位，不信任行号**。每个 任务 第一步都先 grep 定位。
    
*   **命名取自枚举**：新增 span/generation 名一律用 `TraceName`，禁止字符串字面量。
    
*   **观测失败不影响业务**：所有 observation 调用 try/except 兜底；`langfuse_service` 为 None 或 `enabled=False` 时短路。
    
*   **工作目录**：`cdcc-cia-trae-agent/`，命令用 `.venv/bin/python -m pytest ...`。
    

---

## File Structure

### 新建代码

| 文件 | 责任 |
| --- | --- |
| `app/utils/agent_event_tracer.py` | `AgentEventTracer`：单次 agent 执行期间把 AgentScope 事件翻译成 generation/tool 子 observation，维护 reply\_id 配对与防泄漏 |
| `app/utils/agent_event_stream.py` | `iter_agent_events`：async generator helper，包住 `reply_stream` 循环本体 + tracer 消费，两个调用方共用 |

### 新建测试

`tests/test_chat_complete_generation.py`、`test_agent_event_tracer.py`、`test_agent_event_stream.py`、`test_react_step_span.py`、`test_degraded_and_parallel.py`、`test_ttft.py`、`test_pipeline_span.py`

### 修改代码

| 文件 | 改动 | 任务 |
| --- | --- | --- |
| `app/intent/llm_client.py` | `chat_complete` 加 `stage` 参数 + generation | 1 |
| `rewriter.py` / `recognizer.py`(×2) / `react.py` / `chat_service.py` 的 chat\_complete 调用点 | 传 `stage=` | 1 |
| `app/orchestrator/base.py` | `_run_single_agent` 事件循环改用 helper + tracer | 2 |
| `app/services/orchestrator_service.py` | `_run_single_agent_path` 事件循环改用 helper + tracer | 2 |
| `app/orchestrator/react.py` | 每步 react-step span | 3 |
| `app/orchestrator/base.py` | `TaskResult` 加 `metadata` 字段 | 4 |
| `app/services/orchestrator_service.py` | 三处降级 span output 写 degraded | 4 |
| `app/orchestrator/parallel.py` | runner 异常保真写 TaskResult.metadata | 4 |
| `app/services/chat_service.py` | `generate_response` 记 TTFT/totalMs/aborted | 5 |
| `app/orchestrator/pipeline.py` | `run` 包 orchestrate-pipeline span | 6 |

---

### 任务 1: P1-1 chat\_complete 统一 generation

**涉及文件：**

*   修改：`app/intent/llm_client.py`（`chat_complete` 函数）
    
*   修改：5 个调用点 `app/intent/rewriter.py`、`app/intent/recognizer.py`（2 处）、`app/orchestrator/react.py`、`app/services/chat_service.py`
    
*   测试：`tests/test_chat_complete_generation.py`
    

**接口约定：**

*   依赖输入（来自 P0）：`get_current_langfuse() -> Optional[LangfuseService]`；`LangfuseService.start_observation(name, as_type, input, model, metadata)`；`LangfuseService.end_observation(observation, output, usage_details, level, status_message)`
    
*   对外产出：`chat_complete(client, model_config, system_prompt, user_prompt, stage="llm-call") -> str`（新增末位 `stage` 参数，向后兼容）
    
*   [ ] **步骤 1: 定位 + 确认 P0 前提**
    

运行：`.venv/bin/python -c "from app.services.langfuse_service import get_current_langfuse; print('P0 ready')"` 预期：输出 `P0 ready`。若报 ImportError，先执行 P0 后端计划。

运行：`grep -n "async def chat_complete" app/intent/llm_client.py` 预期：定位到函数（当前签名 4 参数，无 stage）。

*   [ ] **步骤 2: 写失败测试**
    

`tests/test_chat_complete_generation.py`：

```python
"""P1-1 chat_complete generation 埋点测试。"""
from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from app.intent.llm_client import chat_complete


def _mock_openai_response(text="hello", prompt_tokens=100, completion_tokens=50):
    resp = MagicMock()
    resp.choices = [MagicMock()]
    resp.choices[0].message.content = text
    resp.usage = MagicMock(prompt_tokens=prompt_tokens, completion_tokens=completion_tokens)
    return resp


def _mock_client(resp):
    client = MagicMock()
    client.chat.completions.create = AsyncMock(return_value=resp)
    return client


@pytest.mark.asyncio
async def test_generation_records_real_usage():
    client = _mock_client(_mock_openai_response("答案", 123, 45))
    lf = MagicMock()
    lf.enabled = True
    obs = MagicMock()
    lf.start_observation.return_value = obs

    with patch("app.intent.llm_client.get_current_langfuse", return_value=lf):
        text = await chat_complete(
            client, {"model_name": "deepseek-v4"},
            system_prompt="sys", user_prompt="usr", stage="llm-query-rewrite",
        )

    assert text == "答案"
    lf.start_observation.assert_called_once()
    start_kwargs = lf.start_observation.call_args.kwargs
    assert start_kwargs["as_type"] == "generation"
    assert start_kwargs["name"] == "llm-query-rewrite"
    assert start_kwargs["model"] == "deepseek-v4"
    end_kwargs = lf.end_observation.call_args.kwargs
    assert end_kwargs["usage_details"] == {"input": 123, "output": 45}


@pytest.mark.asyncio
async def test_no_langfuse_does_not_crash():
    client = _mock_client(_mock_openai_response("x"))
    with patch("app.intent.llm_client.get_current_langfuse", return_value=None):
        text = await chat_complete(client, {"model_name": "m"},
                                   system_prompt="s", user_prompt="u")
    assert text == "x"


@pytest.mark.asyncio
async def test_exception_records_error_level():
    client = MagicMock()
    client.chat.completions.create = AsyncMock(side_effect=ValueError("boom"))
    lf = MagicMock()
    lf.enabled = True
    obs = MagicMock()
    lf.start_observation.return_value = obs

    with patch("app.intent.llm_client.get_current_langfuse", return_value=lf):
        with pytest.raises(ValueError, match="boom"):
            await chat_complete(client, {"model_name": "m"},
                                system_prompt="s", user_prompt="u")

    end_kwargs = lf.end_observation.call_args.kwargs
    assert end_kwargs["level"] == "ERROR"
    assert end_kwargs["status_message"] == "ValueError"

```

*   [ ] **步骤 3: 运行测试确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_chat_complete_generation.py -v` 预期：FAIL — `chat_complete` 不接受 `stage`，或无 `get_current_langfuse` 导入。

*   [ ] **步骤 4: 改写 chat\_complete**
    

将 `app/intent/llm_client.py` 的 `chat_complete` 整体替换为：

```python
async def chat_complete(
    client: AsyncOpenAI,
    model_config: dict,
    system_prompt: str,
    user_prompt: str,
    stage: str = "llm-call",
) -> str:
    """非流式补全，返回完整文本。

    内部创建 Langfuse generation observation，携带真实 token usage。
    observation 通过 OTel 上下文自动挂在当前活跃 span 之下，调用方无需传 langfuse_service。
    """
    model_name = model_config.get("model_name", "deepseek-chat")
    parameters = model_config.get("parameters", {})

    from app.services.langfuse_service import get_current_langfuse
    lf = get_current_langfuse()
    obs = None
    if lf is not None and getattr(lf, "enabled", False):
        obs = lf.start_observation(
            name=stage, as_type="generation", model=model_name,
            input=[{"role": "system", "content": system_prompt},
                   {"role": "user", "content": user_prompt}],
        )
    try:
        response = await client.chat.completions.create(
            model=model_name,
            messages=[
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_prompt},
            ],
            temperature=parameters.get("temperature", 0.1),
            max_tokens=parameters.get("max_tokens", 1024),
            extra_body={"chat_template_kwargs": {"enable_thinking": False}}
        )
        text = response.choices[0].message.content or ""
        if obs is not None and lf is not None:
            usage = getattr(response, "usage", None)
            lf.end_observation(
                obs, output=text,
                usage_details={
                    "input": getattr(usage, "prompt_tokens", 0),
                    "output": getattr(usage, "completion_tokens", 0),
                } if usage else None,
            )
        return text
    except Exception as e:
        if obs is not None and lf is not None:
            lf.end_observation(obs, output={"error": str(e)[:500]},
                               level="ERROR", status_message=type(e).__name__)
        raise

```

*   [ ] **步骤 5: 5 个调用点传 stage**
    

各调用点在 `chat_complete(...)` 末尾加 `stage=`：

*   `app/intent/rewriter.py`（`rewriter.rewrite` 内）：`stage="llm-query-rewrite"`
    
*   `app/intent/recognizer.py`（`recognize_intents` 内，第一处）：`stage="llm-intent-recognition"`
    
*   `app/intent/recognizer.py`（`plan_orchestration` 内，第二处）：`stage="llm-intent-orchestration"`
    
*   `app/orchestrator/react.py`（`_think` 内）：`stage="llm-react-think"`
    
*   `app/services/chat_service.py`（`_generate_recommended_questions` 内，位置参数调用改为补 `stage="llm-recommended-questions"`）
    

用 grep 逐一定位：`grep -rn "chat_complete(" app/ --include="*.py" | grep -v "def chat_complete"`

*   [ ] **步骤 6: 运行测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_chat_complete_generation.py -v` 预期：3 passed

*   [ ] **步骤 7: 提交检查点**
    

改动清单（供手动 commit）：`app/intent/llm_client.py`、5 个调用点文件、`tests/test_chat_complete_generation.py`（新建）

---

### 任务 2: P1-2 AgentEventTracer + iter\_agent\_events helper（双接入点）

**涉及文件：**

*   新建：`app/utils/agent_event_tracer.py`
    
*   新建：`app/utils/agent_event_stream.py`
    
*   修改：`app/orchestrator/base.py`（`_run_single_agent` 事件循环）
    
*   修改：`app/services/orchestrator_service.py`（`_run_single_agent_path` 事件循环）
    
*   测试：`tests/test_agent_event_tracer.py`、`tests/test_agent_event_stream.py`
    

**接口约定：**

*   依赖输入（来自 P0）：`LangfuseService.start_observation(name, as_type, model, metadata)`、`end_observation(observation, output, usage_details, level)`
    
*   依赖输入（AgentScope 2.0.5）：`ModelCallStartEvent{reply_id, model_name}`、`ModelCallEndEvent{reply_id, input_tokens, output_tokens, finished_reason}`、`TextBlockDeltaEvent{reply_id, delta}`、`ToolCallStartEvent{tool_call_id, tool_call_name}`、`ToolResultEndEvent{tool_call_id, state}`、`ExceedMaxItersEvent`
    
*   对外产出：
    
    *   `AgentEventTracer(langfuse_service, agent_id)`，方法 `on_event(event) -> None`、`close() -> None`、`summary() -> dict`
        
    *   `iter_agent_events(agent, user_msg, tracer, on_reply_start) -> AsyncGenerator[AgentEvent, None]`——yield 原始 AgentEvent（调用方负责 append\_event + model\_dump\_json）
        
*   [ ] **步骤 1: 写 AgentEventTracer 失败测试**
    

`tests/test_agent_event_tracer.py`：

```python
"""P1-2 AgentEventTracer 单测：reply_id 配对、悬挂收尾、汇总、异常隔离。"""
from unittest.mock import MagicMock

from app.utils.agent_event_tracer import AgentEventTracer


class FakeModelStart:
    def __init__(self, reply_id, model_name):
        self.reply_id = reply_id; self.model_name = model_name


class FakeModelEnd:
    def __init__(self, reply_id, it, ot, reason="stop"):
        self.reply_id = reply_id; self.input_tokens = it
        self.output_tokens = ot; self.finished_reason = reason


class FakeToolStart:
    def __init__(self, tcid, name):
        self.tool_call_id = tcid; self.tool_call_name = name


class FakeToolEnd:
    def __init__(self, tcid, state):
        self.tool_call_id = tcid; self.state = state


def _tracer():
    lf = MagicMock(); lf.enabled = True
    lf.start_observation.return_value = MagicMock()
    t = AgentEventTracer(lf, "agent-x")
    return t, lf


def test_model_call_pairs_and_usage_summed(monkeypatch):
    import app.utils.agent_event_tracer as m
    monkeypatch.setattr(m, "ModelCallStartEvent", FakeModelStart)
    monkeypatch.setattr(m, "ModelCallEndEvent", FakeModelEnd)
    t, lf = _tracer()
    t.on_event(FakeModelStart("r1", "deepseek-v4"))
    t.on_event(FakeModelEnd("r1", 100, 40))
    assert t.summary()["llmCalls"] == 1
    assert t.summary()["inputTokens"] == 100
    assert t.summary()["outputTokens"] == 40


def test_reply_id_reuse_closes_dangling(monkeypatch):
    """同一 reply_id 再次 START 时，先结束悬挂项，不泄漏。"""
    import app.utils.agent_event_tracer as m
    monkeypatch.setattr(m, "ModelCallStartEvent", FakeModelStart)
    monkeypatch.setattr(m, "ModelCallEndEvent", FakeModelEnd)
    t, lf = _tracer()
    t.on_event(FakeModelStart("r1", "m"))
    t.on_event(FakeModelStart("r1", "m"))   # 复用，触发悬挂收尾
    t.on_event(FakeModelEnd("r1", 1, 1))
    assert t.summary()["llmCalls"] == 2
    assert lf.end_observation.call_count >= 2


def test_tool_failure_counted(monkeypatch):
    import app.utils.agent_event_tracer as m
    monkeypatch.setattr(m, "ToolCallStartEvent", FakeToolStart)
    monkeypatch.setattr(m, "ToolResultEndEvent", FakeToolEnd)
    t, lf = _tracer()
    t.on_event(FakeToolStart("c1", "Bash"))
    t.on_event(FakeToolEnd("c1", "error"))
    assert t.summary()["toolCalls"] == 1
    assert t.summary()["toolFailures"] == 1


def test_close_ends_hanging(monkeypatch):
    import app.utils.agent_event_tracer as m
    monkeypatch.setattr(m, "ModelCallStartEvent", FakeModelStart)
    t, lf = _tracer()
    t.on_event(FakeModelStart("r1", "m"))   # 不 END
    t.close()
    lf.end_observation.assert_called()      # close 收尾悬挂 generation


def test_on_event_swallows_errors():
    """tracer 内部异常不得冒泡影响业务流。"""
    lf = MagicMock(); lf.enabled = True
    lf.start_observation.side_effect = RuntimeError("langfuse down")
    t = AgentEventTracer(lf, "agent-x")

    class _Unknown:  # 不匹配任何 isinstance 分支
        pass
    t.on_event(_Unknown())   # 不抛异常即通过


def test_disabled_is_noop():
    lf = MagicMock(); lf.enabled = False
    t = AgentEventTracer(lf, "agent-x")
    t.on_event(object())
    t.close()
    lf.start_observation.assert_not_called()

```

*   [ ] **步骤 2: 运行确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_agent_event_tracer.py -v` 预期：FAIL — `ModuleNotFoundError: app.utils.agent_event_tracer`

*   [ ] **步骤 3: 实现 AgentEventTracer**
    

`app/utils/agent_event_tracer.py`：

```python
"""把 AgentScope 事件流旁路翻译成 Langfuse 子 observation。

AgentScope 已在事件里给出模型名与真实 token 用量，事件循环原本只转 SSE 就丢弃。
本类在同一循环里旁路消费，把 LLM 调用、工具调用补成可下钻子 observation。
不用 register_hook、不包装 OpenAIChatModel。

配对约定（实测报文验证）：同一 reply_id 下会串行发起多次模型调用，
按 reply_id 维护"当前未结束的 generation"，START 时若存在悬挂项先行结束，防泄漏。
"""
import logging
from typing import Any, Dict, Optional

from agentscope.event import (
    ExceedMaxItersEvent,
    ModelCallEndEvent,
    ModelCallStartEvent,
    TextBlockDeltaEvent,
    ToolCallStartEvent,
    ToolResultEndEvent,
)

logger = logging.getLogger(__name__)


class AgentEventTracer:
    """单次 agent 执行期间的事件翻译器，与 agent span 生命周期一致。"""

    def __init__(self, langfuse_service: Optional[Any], agent_id: str) -> None:
        self._lf = langfuse_service
        self._agent_id = agent_id
        self._model_obs: Dict[str, Any] = {}
        self._model_name: Dict[str, str] = {}
        self._text_buf: Dict[str, list] = {}
        self._tool_obs: Dict[str, Any] = {}
        self._tool_name: Dict[str, str] = {}
        self.input_tokens = 0
        self.output_tokens = 0
        self.llm_calls = 0
        self.tool_calls = 0
        self.tool_failures = 0
        self.exceed_max_iters = False

    @property
    def _enabled(self) -> bool:
        return bool(self._lf is not None and getattr(self._lf, "enabled", False))

    def on_event(self, event: Any) -> None:
        if not self._enabled:
            return
        try:
            self._dispatch(event)
        except Exception:
            logger.debug("[AgentEventTracer] 事件埋点失败", exc_info=True)

    def close(self) -> None:
        if not self._enabled:
            return
        for reply_id in list(self._model_obs):
            self._end_model(reply_id, None, None, "interrupted")
        for tool_call_id in list(self._tool_obs):
            self._end_tool(tool_call_id, "interrupted")

    def summary(self) -> dict:
        return {
            "llmCalls": self.llm_calls,
            "toolCalls": self.tool_calls,
            "toolFailures": self.tool_failures,
            "inputTokens": self.input_tokens,
            "outputTokens": self.output_tokens,
            "exceedMaxIters": self.exceed_max_iters,
        }

    def _dispatch(self, event: Any) -> None:
        if isinstance(event, ModelCallStartEvent):
            self._end_model(event.reply_id, None, None, "dangling")
            self._model_name[event.reply_id] = event.model_name

            self._text_buf[event.reply_id] = [ ]

            self._model_obs[event.reply_id] = self._lf.start_observation(
                name=f"llm-{event.model_name}", as_type="generation",
                model=event.model_name,
                metadata={"agentId": self._agent_id, "replyId": event.reply_id},
            )
            self.llm_calls += 1
        elif isinstance(event, ModelCallEndEvent):
            self.input_tokens += event.input_tokens or 0
            self.output_tokens += event.output_tokens or 0
            self._end_model(
                event.reply_id, event.input_tokens, event.output_tokens,
                getattr(event.finished_reason, "value", str(event.finished_reason)),
            )
        elif isinstance(event, TextBlockDeltaEvent):
            buf = self._text_buf.get(event.reply_id)
            if buf is not None and len(buf) < 2000:
                buf.append(event.delta or "")
        elif isinstance(event, ToolCallStartEvent):
            self._tool_name[event.tool_call_id] = event.tool_call_name
            self._tool_obs[event.tool_call_id] = self._lf.start_observation(
                name=f"tool-{event.tool_call_name}", as_type="tool",
                metadata={"agentId": self._agent_id, "toolCallId": event.tool_call_id},
            )
            self.tool_calls += 1
        elif isinstance(event, ToolResultEndEvent):
            state = getattr(event.state, "value", str(event.state))
            if state == "error":
                self.tool_failures += 1
            self._end_tool(event.tool_call_id, state)
        elif isinstance(event, ExceedMaxItersEvent):
            self.exceed_max_iters = True

    def _end_model(self, reply_id, input_tokens, output_tokens, finished_reason) -> None:
        obs = self._model_obs.pop(reply_id, None)
        if obs is None:
            return

        output_text = "".join(self._text_buf.pop(reply_id, []))

        usage = None
        if input_tokens is not None or output_tokens is not None:
            usage = {"input": input_tokens or 0, "output": output_tokens or 0}
        self._lf.end_observation(
            obs, output={"text": output_text[:4000], "finishedReason": finished_reason},
            usage_details=usage,
        )
        self._model_name.pop(reply_id, None)

    def _end_tool(self, tool_call_id, state) -> None:
        obs = self._tool_obs.pop(tool_call_id, None)
        if obs is None:
            return
        self._lf.end_observation(
            obs, output={"state": state, "tool": self._tool_name.pop(tool_call_id, "")},
            level="ERROR" if state == "error" else None,
        )

```

*   [ ] **步骤 4: 运行 tracer 测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_agent_event_tracer.py -v` 预期：6 passed

*   [ ] **步骤 5: 写 helper 失败测试**
    

`tests/test_agent_event_stream.py`：

```python
"""P1-2 iter_agent_events helper 单测：事件透传 + ReplyStart 回调 + tracer 喂入。"""
from unittest.mock import MagicMock

import pytest

from app.utils.agent_event_stream import iter_agent_events


class FakeReplyStart:
    def __init__(self, name, reply_id):
        self.name = name; self.reply_id = reply_id


class FakeAgentEvent:
    def __init__(self, tag):
        self.tag = tag


class FakeAgent:
    def __init__(self, events):
        self._events = events

    async def reply_stream(self, user_msg):
        for e in self._events:
            yield e


@pytest.mark.asyncio
async def test_yields_agent_events_and_feeds_tracer(monkeypatch):
    import app.utils.agent_event_stream as m
    monkeypatch.setattr(m, "ReplyStartEvent", FakeReplyStart)
    monkeypatch.setattr(m, "AgentEvent", FakeAgentEvent)
    rs = FakeReplyStart("bot", "r1")
    e1 = FakeAgentEvent("a"); e2 = FakeAgentEvent("b")
    agent = FakeAgent([rs, e1, e2])
    tracer = MagicMock()

    started = [ ]



    out = [ ]

    async for ev in iter_agent_events(agent, "msg", tracer, lambda e: started.append(e)):
        out.append(ev)

    assert out == [e1, e2]              # 只 yield AgentEvent，ReplyStart 走回调
    assert started == [rs]
    assert tracer.on_event.call_count == 2

```

*   [ ] **步骤 6: 运行确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_agent_event_stream.py -v` 预期：FAIL — `ModuleNotFoundError`

*   [ ] **步骤 7: 实现 helper**
    

`app/utils/agent_event_stream.py`：

```python
"""reply_stream 事件循环公共 helper，base.py 与 orchestrator_service.py 单 agent 路径共用。

只抽循环本体：消费 reply_stream → ReplyStart 走回调 → AgentEvent 喂 tracer 后 yield。
span 创建与收尾、append_event、model_dump_json 均留在调用方（两处收尾逻辑不同）。
"""
from typing import Any, AsyncGenerator, Callable

from agentscope.event import AgentEvent, ReplyStartEvent


async def iter_agent_events(
    agent: Any,
    user_msg: Any,
    tracer: Any,
    on_reply_start: Callable[[Any], None],
) -> AsyncGenerator[Any, None]:
    """yield 每个 AgentEvent（原始对象）。调用方负责 append_event + model_dump_json。"""
    async for event in agent.reply_stream(user_msg):
        if isinstance(event, ReplyStartEvent):
            on_reply_start(event)
        if isinstance(event, AgentEvent):
            tracer.on_event(event)
            yield event

```

*   [ ] **步骤 8: 运行 helper 测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_agent_event_stream.py -v` 预期：1 passed

*   [ ] **步骤 9: 接入 base.py** 
    

grep 定位：`grep -n "async for event in agent.reply_stream" app/orchestrator/base.py`

将 `with span_ctx as agent_span:` 内的 `try:` 块改造——原循环：

```python
        with span_ctx as agent_span:
            try:
                async for event in agent.reply_stream(user_msg):
                    if isinstance(event, ReplyStartEvent):

                        apply = AssistantMsg(name=event.name, content=[], id=event.reply_id)

                    if isinstance(event, AgentEvent):
                        if apply:
                            apply.append_event(event)
                        yield f"data: {event.model_dump_json()}\n\n"

```

改为（引入 tracer + helper，收尾逻辑不变）：

```python
        from app.utils.agent_event_tracer import AgentEventTracer
        from app.utils.agent_event_stream import iter_agent_events
        tracer = AgentEventTracer(langfuse_service, agent_id)
        with span_ctx as agent_span:
            try:
                def _on_reply_start(ev):
                    nonlocal apply

                    apply = AssistantMsg(name=ev.name, content=[], id=ev.reply_id)


                async for event in iter_agent_events(agent, user_msg, tracer, _on_reply_start):
                    if apply:
                        apply.append_event(event)
                    yield f"data: {event.model_dump_json()}\n\n"

```

在 `except Exception` 之后、`if agent_span:` 之前插入 `finally: tracer.close()`；并把 `agent_span.update(output={...})` 的 dict 合并 `**tracer.summary()`：

```python
            except Exception as e:
                logger.exception(f"[Orchestrator] 智能体 {agent_id} 执行异常")
                result.success = False
                result.output = f"执行出错: {str(e)}"
            finally:
                tracer.close()

            if agent_span:
                try:
                    agent_span.update(output={
                        "output": result.output,
                        "success": result.success,
                        **tracer.summary(),
                    })
                except Exception:
                    pass

```

*   [ ] **步骤 10: 接入 orchestrator\_service.py** 
    

grep 定位：`grep -n "async for event in agent.reply_stream" app/services/orchestrator_service.py`

同样改造该处循环（收尾是 `_persist_agent_state` + `_last_agent_ids`，保持不变），循环体替换为 helper：

```python
        from app.utils.agent_event_tracer import AgentEventTracer
        from app.utils.agent_event_stream import iter_agent_events
        tracer = AgentEventTracer(langfuse_service, agent_id)
        with self._span(langfuse_service, f"agent-{agent_id}",
                        {"intent": intent.id, "query": user_input}) as agent_span:
            try:
                def _on_reply_start(ev):
                    nonlocal apply

                    apply = AssistantMsg(name=ev.name, content=[], id=ev.reply_id)


                async for event in iter_agent_events(agent, user_msg, tracer, _on_reply_start):
                    if apply:
                        apply.append_event(event)
                    yield f"data: {event.model_dump_json()}\n\n"

                # ... 原有 text 提取 / _persist_agent_state / _last_agent_ids 不变 ...
                _safe_update_span(agent_span, {
                    "output": final_output_parts[0] if final_output_parts else "",
                    "success": True,
                    **tracer.summary(),
                })
            except Exception as e:
                # ... 原有异常处理不变 ...
                self._last_success = False
                # yield error 事件（原样）
            finally:
                tracer.close()

```

*   [ ] **步骤 11: 全量回归 + 导入验证**
    

运行：`.venv/bin/python -c "from app.orchestrator.base import BaseOrchestrator; from app.services.orchestrator_service import OrchestratorService; print('OK')"` 预期：OK

运行：`.venv/bin/python -m pytest tests/test_agent_event_tracer.py tests/test_agent_event_stream.py -v` 预期：7 passed

*   [ ] **步骤 12: 提交检查点**
    

改动清单：`app/utils/agent_event_tracer.py`（新建）、`app/utils/agent_event_stream.py`（新建）、`app/orchestrator/base.py`、`app/services/orchestrator_service.py`、两个测试文件（新建）

---

### 任务 3: P1-3 ReAct 每轮 span（平级、duration 准确）

**涉及文件：**

*   修改：`app/orchestrator/react.py`（`run` 循环体）
    
*   测试：`tests/test_react_step_span.py`
    

**接口约定：**

*   依赖输入（来自 P0）：`TraceName.REACT_STEP`（`"react-step-{step}"`）；`LangfuseService.start_observation/end_observation`
    
*   依赖 任务 1：`_think` 内的 LLM 已成 `llm-react-think` generation（与 react-step 平级）
    
*   对外产出：每步一个 `react-step-{n}` span，output 含 `action`/`isFinal`/`observationPreview`/`durationMs`；截断时含 `exceeded`
    

**设计要点**：**不用** `**with**` **包循环体**（实测 `with` 跨 `yield` 会把下游 SSE 消费时间计入 duration，且 break 中断时 finally 延迟到 GC）。改为每步逻辑走完后一次性 `start_observation`+`end_observation`，`step_t0` 只覆盖纯计算。

*   [ ] **步骤 1: 写失败测试**
    

`tests/test_react_step_span.py`：

```python
"""P1-3 ReAct 每轮 span 单测。"""
import pytest
from unittest.mock import AsyncMock, MagicMock

from app.orchestrator.react import ReActOrchestrator
from app.intent.models import Intent, IntentResult


def _orch(monkeypatch):
    factory = MagicMock()
    orch = ReActOrchestrator(
        agent_factory=factory, think_client=MagicMock(),
        think_model_config={}, think_prompt="{{task}}{{scratch}}{{available_actions}}",
        max_steps=3,
    )
    return orch


@pytest.mark.asyncio
async def test_each_step_emits_span(monkeypatch):
    orch = _orch(monkeypatch)
    # 第 1 步 final，直接结束
    orch._think = AsyncMock(return_value={"is_final": True, "conclusion": "done", "thought": "t"})
    lf = MagicMock(); lf.enabled = True
    obs = MagicMock(); lf.start_observation.return_value = obs

    ir = IntentResult(rewritten_query="q", intents=[Intent(id="i1", query="q", agent="a")],

                      relation="react", execution_order=[])


    events = [ ]

    async for ev in orch.run(ir, langfuse_service=lf):
        events.append(ev)

    lf.start_observation.assert_called()
    name = lf.start_observation.call_args_list[0].kwargs["name"]
    assert name == "react-step-1"
    out = lf.end_observation.call_args_list[0].kwargs["output"]
    assert out["isFinal"] is True
    assert "durationMs" in out


@pytest.mark.asyncio
async def test_no_langfuse_no_crash(monkeypatch):
    orch = _orch(monkeypatch)
    orch._think = AsyncMock(return_value={"is_final": True, "conclusion": "c", "thought": "t"})
    ir = IntentResult(rewritten_query="q", intents=[Intent(id="i1", query="q", agent="a")],

                      relation="react", execution_order=[])

    out = [ev async for ev in orch.run(ir, langfuse_service=None)]
    assert any("react_final" in e or "summary" in e for e in out)

```

*   [ ] **步骤 2: 运行确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_react_step_span.py -v` 预期：FAIL — 无 span 产生（当前 react.py 循环无 observation 调用）。

*   [ ] **步骤 3: 改造 react.py 循环**
    

grep 定位：`grep -n "for step in range" app/orchestrator/react.py`

在 `app/orchestrator/react.py` 顶部 import 补 `import time` 与 `from app.utils.trace_names import TraceName`。

在 `for step in range(1, self._max_steps + 1):` 循环体最前面记起点，并在每个 return/循环末尾前收 span。由于该循环有多个提前 `return`（final、无 action），用一个内嵌函数统一收 span，避免遗漏：

```python
        for step in range(1, self._max_steps + 1):
            step_t0 = time.perf_counter()

            def _emit_step_span(action="", is_final=False, observation_preview="", exceeded=False):
                if not (langfuse_service and getattr(langfuse_service, "enabled", False)):
                    return
                try:
                    obs = langfuse_service.start_observation(
                        name=TraceName.REACT_STEP.format(step=step), as_type="span",
                        input={"step": step, "max_steps": self._max_steps})
                    out = {"action": action, "isFinal": is_final,
                           "observationPreview": observation_preview[:200],
                           "durationMs": int((time.perf_counter() - step_t0) * 1000)}
                    if exceeded:
                        out["exceeded"] = True
                    langfuse_service.end_observation(obs, output=out)
                except Exception:
                    pass

            yield self._event({"type": "react_step", "step": step, "max_steps": self._max_steps})

            thought = await self._think(task_desc, scratch, available_actions)

            if thought.get("is_final"):
                conclusion = thought.get("conclusion", "")
                _emit_step_span(action="final", is_final=True, observation_preview=conclusion)
                yield self._event({"type": "react_final", "step": step,
                                   "thought": thought.get("thought", ""), "conclusion": conclusion})
                yield self._event({"type": "summary", "content": conclusion})
                return

            action = thought.get("next_action")
            if not action:
                _emit_step_span(action="", is_final=True, observation_preview="无法确定下一步动作")
                yield self._event({"type": "react_final", "step": step,
                                   "thought": "无法确定下一步动作",
                                   "conclusion": "抱歉，当前任务过于复杂，无法自动完成。请尝试将问题拆分后分别提问。"})
                return

            action_name = action.get("name", "")
            action_args = action.get("args", {})
            yield self._event({"type": "react_act", "step": step,
                               "action": action_name, "thought": thought.get("thought", "")})

            observation = ""
            async for item in self._execute_action(
                action_name, action_args, intent_result, session_id, agent_states, langfuse_service,
            ):
                if isinstance(item, TaskResult):
                    observation = item.output if item.success else f"执行失败: {item.output}"
                else:
                    yield item

            scratch += (f"\n--- 步骤 {step} ---\n"
                        f"Thought: {thought.get('thought', '')}\n"
                        f"Action: {action_name} ({json.dumps(action_args, ensure_ascii=False)})\n"
                        f"Observation: {observation}\n")

            _emit_step_span(action=action_name, is_final=False, observation_preview=observation)
            yield self._event({"type": "react_observe", "step": step,
                               "observation_preview": observation[:200]})

        # 步数超限兜底：补一条 exceeded span
        if langfuse_service and getattr(langfuse_service, "enabled", False):
            try:
                obs = langfuse_service.start_observation(
                    name=TraceName.REACT_STEP.format(step=self._max_steps), as_type="span",
                    input={"exceeded": True, "max_steps": self._max_steps})
                langfuse_service.end_observation(obs, output={"exceeded": True, "maxSteps": self._max_steps})
            except Exception:
                pass
        yield self._event({"type": "react_timeout", "max_steps": self._max_steps})
        yield self._event({"type": "summary",
                           "content": "推理步数已达上限，请尝试将问题简化或分步骤提问。"})

```
> 注：`_emit_step_span` 内嵌函数每步重定义以闭包捕获 `step`/`step_t0`，逻辑简单可接受；如实现者偏好，可提为带 step 参数的方法，等价即可。

*   [ ] **步骤 4: 运行测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_react_step_span.py -v` 预期：2 passed

*   [ ] **步骤 5: 提交检查点**
    

改动清单：`app/orchestrator/react.py`、`tests/test_react_step_span.py`（新建）

---

### 任务 4: P1-4 降级原因入 trace + parallel 异常保真

**涉及文件：**

*   修改：`app/orchestrator/base.py`（`TaskResult` 加 `metadata` 字段）
    
*   修改：`app/services/orchestrator_service.py`（三处降级）
    
*   修改：`app/orchestrator/parallel.py`（runner 异常保真）
    
*   测试：`tests/test_degraded_and_parallel.py`
    

**接口约定：**

*   依赖输入（来自 P0）：`_safe_update_span`（现有）
    
*   对外产出：`TaskResult.metadata: Optional[dict] = None`（新增字段）；三处降级 span output 含 `degraded`/`reason`/`detail`；parallel 失败 TaskResult.metadata 含 `errorClass`/`traceback`
    

> **关键前提**：`TaskResult`（base.py）当前**无 metadata 字段**，Step 3 必须先加，否则 parallel 保真无处可写。

*   [ ] **步骤 1: 写失败测试**
    

`tests/test_degraded_and_parallel.py`：

```python
"""P1-4 降级入 trace + parallel 异常保真。"""
from app.orchestrator.base import TaskResult


def test_taskresult_has_metadata_field():
    r = TaskResult(intent_id="i", agent_id="a", metadata={"errorClass": "ValueError"})
    assert r.metadata == {"errorClass": "ValueError"}


def test_taskresult_metadata_defaults_none():
    r = TaskResult(intent_id="i", agent_id="a")
    assert r.metadata is None

```

*   [ ] **步骤 2: 运行确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_degraded_and_parallel.py -v` 预期：FAIL — `TaskResult` 无 `metadata` 字段。

*   [ ] **步骤 3: TaskResult 加 metadata 字段**
    

grep 定位：`grep -n "final_state: Optional" app/orchestrator/base.py`

在 `app/orchestrator/base.py` 的 `TaskResult` 类中 `final_state` 之后加：

```python
    metadata: Optional[dict] = None

```

*   [ ] **步骤 4: 运行测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_degraded_and_parallel.py -v` 预期：2 passed

*   [ ] **步骤 5: 三处降级写 degraded**
    

grep 定位：`grep -n "logger.exception.*降级\|使用原始输入" app/services/orchestrator_service.py`

三处 `except Exception:` 后各补一行 `_safe_update_span`，把降级原因写入对应 span（rw\_span/rec\_span/orch\_span）。因 `_safe_update_span` 只接受一个 output dict，改为在 except 内先记录降级信息，再在原 `_safe_update_span` 调用里合并：

查询改写（`as rw_span:` 块）：

```python
            try:
                rewritten = await rewriter.rewrite(user_input, history)
                _rw_extra = {}
            except Exception as e:
                logger.exception("[OrchestratorService] 查询改写失败，使用原始输入")
                rewritten = user_input
                _rw_extra = {"degraded": True, "reason": type(e).__name__, "detail": str(e)[:500]}
            _safe_update_span(rw_span, {"rewritten": rewritten, **_rw_extra})

```

意图识别（`as rec_span:` 块）：

```python
            try:
                intents = await recognizer.recognize_intents(rewritten, history)
                _rec_extra = {}
            except Exception as e:
                logger.exception("[OrchestratorService] 意图识别失败，降级为 general_chat")
                intents = [Intent(id="general_chat", query=rewritten, agent="general_agent")]
                _rec_extra = {"degraded": True, "reason": type(e).__name__, "detail": str(e)[:500]}
            _safe_update_span(rec_span, {
                "intents": [{"id": i.id, "agent": i.agent} for i in intents], **_rec_extra,
            })

```

意图编排（`as orch_span:` 块）：

```python
            try:
                relation, execution_order = await recognizer.plan_orchestration(rewritten, intents)
                _orch_extra = {}
            except Exception as e:
                logger.exception("[OrchestratorService] 意图编排失败，降级为 independent")

                relation, execution_order = "independent", [ ]

                _orch_extra = {"degraded": True, "reason": type(e).__name__, "detail": str(e)[:500]}
            _safe_update_span(orch_span, {
                "relation": relation, "execution_order": execution_order, **_orch_extra,
            })

```

*   [ ] **步骤 6: parallel runner 异常保真**
    

grep 定位：`grep -n "async def runner\|执行异常" app/orchestrator/parallel.py`

`app/orchestrator/parallel.py` 顶部 import 补 `import traceback`。将 runner 的 `except Exception as e:` 分支的 TaskResult 补 metadata：

```python
            except Exception as e:
                logger.exception(
                    f"[ParallelOrchestrator] 意图 {intent.id} 执行异常"
                )
                await queue.put(TaskResult(
                    intent_id=intent.id,
                    agent_id=agent_id,
                    success=False,
                    output=f"执行异常: {str(e)}",
                    metadata={
                        "errorClass": f"{type(e).__module__}.{type(e).__name__}",
                        "traceback": traceback.format_exc()[-1000:],
                    },
                ))

```

*   [ ] **步骤 7: 补 parallel 保真测试**
    

在 `tests/test_degraded_and_parallel.py` 末尾追加：

```python
import asyncio
import pytest
from unittest.mock import MagicMock

from app.orchestrator.parallel import ParallelOrchestrator
from app.intent.models import Intent, IntentResult


@pytest.mark.asyncio
async def test_parallel_runner_preserves_exception():
    orch = ParallelOrchestrator(agent_factory=MagicMock(), timeout=5.0)

    async def _boom(*a, **k):
        raise ValueError("branch boom")
        yield  # make it an async gen
    orch._run_single_agent = _boom

    ir = IntentResult(rewritten_query="q",
                      intents=[Intent(id="i1", query="q", agent="a")],

                      relation="independent", execution_order=[])


    results = [ ]

    async for ev in orch.run(ir):
        pass
    # runner 把异常写入 _last_results 的 metadata
    failed = [r for r in orch._last_results if not r.success]
    assert failed and failed[0].metadata is not None
    assert "ValueError" in failed[0].metadata["errorClass"]

```

*   [ ] **步骤 8: 全量运行**
    

运行：`.venv/bin/python -m pytest tests/test_degraded_and_parallel.py -v` 预期：3 passed

运行：`.venv/bin/python -c "from app.services.orchestrator_service import OrchestratorService; from app.orchestrator.parallel import ParallelOrchestrator; print('OK')"` 预期：OK

*   [ ] **步骤 9: 提交检查点**
    

改动清单：`app/orchestrator/base.py`、`app/services/orchestrator_service.py`、`app/orchestrator/parallel.py`、`tests/test_degraded_and_parallel.py`（新建）

---

### 任务 5: P1-5 TTFT 与端到端耗时

**涉及文件：**

*   修改：`app/services/chat_service.py`（`generate_response`）
    
*   测试：`tests/test_ttft.py`
    

**接口约定：**

*   依赖输入（来自 P0）：根 span `root_obs`；`_safe_update_span`
    
*   对外产出：根 span output 增 `ttftMs`/`totalMs`/`aborted`/`abortedAtMs`
    

> **依赖 P0-3/P0-5**：P0 已在 `generate_response` 里加 `trace_emitted` 与早期 trace\_ready，本 任务 在同一函数追加计时，注意不与 P0 改动冲突（P0 改的是 `with root_span_ctx` 入口，本 任务 改的是循环内首个内容事件与 finally）。

*   [ ] **步骤 1: 写失败测试**
    

`tests/test_ttft.py`：

```python
"""P1-5 TTFT 计时单测：验证辅助函数正确识别首个内容事件并计时。"""
import time

from app.services.chat_service import _compute_ttft_marker


def test_first_content_event_marks_ttft():
    """辅助：给定事件类型序列，首个 TEXT_BLOCK_DELTA/summary 触发 TTFT。"""
    assert _compute_ttft_marker("TEXT_BLOCK_DELTA") is True
    assert _compute_ttft_marker("summary") is True
    assert _compute_ttft_marker("react_step") is False
    assert _compute_ttft_marker("task_start") is False

```

*   [ ] **步骤 2: 运行确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_ttft.py -v` 预期：FAIL — `_compute_ttft_marker` 未定义。

*   [ ] **步骤 3: 加辅助函数 + 接入 generate\_response**
    

grep 定位：`grep -n "async def generate_response\|t0 = time\|final_output = " app/services/chat_service.py`

在 `app/services/chat_service.py` 模块级（`_estimate_tokens` 附近）加纯函数，便于单测：

```python
def _compute_ttft_marker(event_type: str) -> bool:
    """判断某事件类型是否是首个用户可见内容事件（用于 TTFT 计时）。"""
    return event_type in ("TEXT_BLOCK_DELTA", "summary")

```

确认 `generate_response` 顶部已 `import time`（P0 可能已引入；否则补）。在 `aborted = False` 附近加计时变量：

```python
    import time as _time
    t0 = _time.perf_counter()
    ttft_ms = None
    aborted_at_ms = None

```

在事件循环内解析出 `event_type` 之后（P0 已有 `event_type = payload.get("type", "")`），补 TTFT 捕获：

```python
                        if ttft_ms is None and _compute_ttft_marker(event_type):
                            ttft_ms = int((_time.perf_counter() - t0) * 1000)

```

在取消分支（`if _cancelled(): aborted = True` 处）记录中断时点：

```python
                if _cancelled():
                    aborted = True
                    aborted_at_ms = int((_time.perf_counter() - t0) * 1000)
                    raise asyncio.CancelledError()

```

在根 span 收尾处（P0 的 `_safe_update_span(root_obs, {...})` 调用；grep `_safe_update_span(root_obs`），把 output dict 合并计时字段：

```python
            _safe_update_span(root_obs, {
                "reply": final_output,
                "session_id": session_id,
                "user_id": user_id,
                "ttftMs": ttft_ms,
                "totalMs": int((_time.perf_counter() - t0) * 1000),
                "aborted": aborted,
                "abortedAtMs": aborted_at_ms,
            })

```
> 若 P0 版本的根 span 收尾 dict 字段名不同，以实际为准合并这 4 个计时字段。

*   [ ] **步骤 4: 运行测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_ttft.py -v` 预期：4 passed

运行：`.venv/bin/python -c "from app.services.chat_service import generate_response, _compute_ttft_marker; print('OK')"` 预期：OK

*   [ ] **步骤 5: 提交检查点**
    

改动清单：`app/services/chat_service.py`、`tests/test_ttft.py`（新建）

---

### 任务 6: P1-6 pipeline 编排级 span

**涉及文件：**

*   修改：`app/orchestrator/pipeline.py`（`run`）
    
*   测试：`tests/test_pipeline_span.py`
    

**接口约定：**

*   依赖输入（来自 P0）：`LangfuseService.start_observation/end_observation`；`TraceName`（新增成员 `ORCHESTRATE_PIPELINE`，见 Step 3）
    
*   对外产出：`orchestrate-pipeline` span，output 含 `totalSteps`/`stoppedAtStep`/`success`
    

> pipeline 有提前 `return`（失败终止）与正常完成两个出口，用 try/finally 保证 span 一定收尾并记录 `stoppedAtStep`。

*   [ ] **步骤 1: 写失败测试**
    

`tests/test_pipeline_span.py`：

```python
"""P1-6 pipeline 编排级 span 单测。"""
import pytest
from unittest.mock import MagicMock

from app.orchestrator.pipeline import PipelineOrchestrator
from app.orchestrator.base import TaskResult
from app.intent.models import Intent, IntentResult


def _ir(n):
    return IntentResult(
        rewritten_query="q",
        intents=[Intent(id=f"i{k}", query="q", agent="a") for k in range(n)],

        relation="pipeline", execution_order=[])



@pytest.mark.asyncio
async def test_pipeline_span_records_total_and_stopped(monkeypatch):
    orch = PipelineOrchestrator(agent_factory=MagicMock(), step_timeout=5.0)

    # 第 1 步失败 → 流水线在第 1 步终止
    async def _fail_first(intent, **k):
        yield TaskResult(intent_id=intent.id, agent_id="a", success=False, output="boom")
    orch._run_single_agent = _fail_first

    lf = MagicMock(); lf.enabled = True
    obs = MagicMock(); lf.start_observation.return_value = obs

    async for _ in orch.run(_ir(3), langfuse_service=lf):
        pass

    lf.start_observation.assert_called_once()
    assert lf.start_observation.call_args.kwargs["name"] == "orchestrate-pipeline"
    out = lf.end_observation.call_args.kwargs["output"]
    assert out["totalSteps"] == 3
    assert out["stoppedAtStep"] == 1
    assert out["success"] is False

```

*   [ ] **步骤 2: 运行确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_pipeline_span.py -v` 预期：FAIL — 无 `orchestrate-pipeline` span。

*   [ ] **步骤 3: 加 TraceName 成员 + 改造 pipeline.run**
    

先在 `app/utils/trace_names.py` 的 `TraceName` 加一个成员（收尾与外部依赖区）：

```python
    ORCHESTRATE_PIPELINE = "orchestrate-pipeline"

```

`app/orchestrator/pipeline.py` 顶部 import 补 `import time` 与 `from app.utils.trace_names import TraceName`。grep 定位 `grep -n "async def run\|for i, intent in enumerate\|return$" app/orchestrator/pipeline.py`。

将 `run` 的串行循环用 span 包裹（span 在调用方层级创建，非 context-manager 以便两个出口统一收尾）：

```python
        # P1-6: 编排级 span
        _pl_obs = None
        _stopped_at = None
        if langfuse_service and getattr(langfuse_service, "enabled", False):
            try:
                _pl_obs = langfuse_service.start_observation(
                    name=TraceName.ORCHESTRATE_PIPELINE, as_type="span",
                    input={"totalSteps": len(intents)})
            except Exception:
                _pl_obs = None

        def _close_pipeline_span(success: bool):
            if _pl_obs is None:
                return
            try:
                langfuse_service.end_observation(_pl_obs, output={
                    "totalSteps": len(intents),
                    "stoppedAtStep": _stopped_at,
                    "success": success,
                })
            except Exception:
                pass

        prior_context = ""

        self._last_results = [ ]

        try:
            for i, intent in enumerate(intents):
                # ... 原有 task_start / _run_single_agent / task_end 逻辑不变 ...
                self._last_results.append(result)
                # task_end 事件（原样）

                if not result.success:
                    _stopped_at = i + 1
                    yield self._event({
                        "type": "pipeline_intercept", "step": i + 1,
                        "intent_id": intent.id,
                        "message": f"步骤 {i + 1}（{intent.id}）执行失败，流水线终止：{result.output}",
                    })
                    _close_pipeline_span(success=False)
                    return

                prior_context = f"\n{result.output}"

            _stopped_at = len(intents)
            _close_pipeline_span(success=True)
            yield self._event({"type": "summary", "content": prior_context.strip()})
        except Exception:
            _close_pipeline_span(success=False)
            raise

```
> 实现者保留循环体内 `task_start`/`_run_single_agent`/超时兜底/`task_end` 的原有代码，仅插入 `_stopped_at` 赋值与 `_close_pipeline_span` 调用、以及外层 try/except。

*   [ ] **步骤 4: 运行测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_pipeline_span.py -v` 预期：1 passed

*   [ ] **步骤 5: 全量回归**
    

运行：`.venv/bin/python -m pytest tests/ -v` 预期：全部通过（任务 1-6 累计：3 + 6 + 1 + 2 + 3 + 4 + 1 = 20 项 P1 用例，加 P0 用例）

*   [ ] **步骤 6: 提交检查点**
    

改动清单：`app/utils/trace_names.py`、`app/orchestrator/pipeline.py`、`tests/test_pipeline_span.py`（新建）

---

## 自检

### 1. 需求覆盖（对照设计文档六项）

| 设计条目 | 任务 | 状态 |
| --- | --- | --- |
| P1-1 chat\_complete generation | 任务 1 | 覆盖 |
| P1-2 AgentEventTracer + helper 双接入点 | 任务 2 | 覆盖（base.py + orchestrator\_service.py 两处） |
| P1-3 ReAct 每轮 span（平级、duration 准确） | 任务 3 | 覆盖 |
| P1-4 降级入 trace + parallel 异常保真 | 任务 4 | 覆盖（含 TaskResult.metadata 前置） |
| P1-5 TTFT 与端到端耗时 | 任务 5 | 覆盖 |
| P1-6 pipeline 编排级 span | 任务 6 | 覆盖 |

### 2. 占位符扫描

*   无 "TBD/TODO/implement later"；所有 代码步骤 含完整可粘贴代码。
    
*   任务 3/6 中"原有逻辑不变"处均明确指出保留哪段、插入什么，非占位。
    

### 3. 类型一致性

*   `chat_complete(..., stage="llm-call")`（任务 1）— 5 个调用点传 `stage=`（任务 1 Step 5）一致。
    
*   `AgentEventTracer(langfuse_service, agent_id)` / `on_event` / `close` / `summary`（任务 2）— base.py 与 orchestrator\_service.py 调用一致（任务 2 Step 9/10）。
    
*   `iter_agent_events(agent, user_msg, tracer, on_reply_start)`（任务 2）— 两处调用签名一致。
    
*   `TaskResult.metadata: Optional[dict]`（任务 4 Step 3）— parallel 写入（Step 6）与测试（Step 1）一致。
    
*   `TraceName.REACT_STEP`（任务 3）、`TraceName.ORCHESTRATE_PIPELINE`（任务 6 Step 3 新增）— 均取自枚举，无字面量。
    
*   `_compute_ttft_marker(event_type) -> bool`（任务 5）— 定义与调用一致。
    

### 4. 依赖与顺序

*   任务 1、2、4、6 相互独立；任务 3 依赖 任务 1（生成 llm-react-think）；任务 5 依赖 P0 根 span。
    
*   全部 6 项依赖 P0 已落地（Global Constraints 已声明硬前提，任务 1 Step 1 有运行时校验）。
    

---

_计划完成。_