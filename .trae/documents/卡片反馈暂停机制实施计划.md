# 卡片反馈暂停机制实施计划

**目标：** 实现确认组件/表单组件的「agent 发卡片 → 同轮暂停 → 用户卡片反馈 → 同轮恢复」闭环，并以本地 mock 组件配置完成完整流程串联验证。

**架构：** 三通道结构——配置通道（`/api/presentation/custom-components`，AUTH\_MOCK 时读本地 mock JSON）、事件通道（SSE `CUSTOM_COMPONENT`，现有链路零改动）、反馈通道（新增 `POST /chat/feedback`）。挂起点在工具执行层：`render_feedback_card` 异步生成器工具先 yield 卡片 chunk，再 `await` 进程内 `asyncio.Event`，被回传端点唤醒后 yield 用户反馈，agent 在同一轮 ReAct 循环内继续。

**技术栈：** 后端 FastAPI + AgentScope 2.0.5（`FunctionTool` + `AsyncGenerator[ToolChunk]`）+ pytest/pytest-asyncio；前端 React 19 + Zustand + Vitest + @testing-library/react。

## 全局约束

**相对 spec 的两处收敛**（实施口径，原因如下）：

1.  `POST /chat/feedback` 的 409 收敛为 404——`wait_feedback` 返回即从注册表 pop，重复提交必然落到「不存在」分支，区分 409 的窗口极小，不值得引入第三种状态；
    
2.  `/chat/stop` 端点不改——`cancel_event` 触发流终止后，`chat.py` 流的 `finally` 会联动 `cancel_feedback`，无需在 stop 里重复处理。
    

## 文件结构总览

后端（cdcc-cia-trae-agent）：

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `app/services/feedback_service.py` | 新建 | 挂起注册表：wait/resolve/cancel + 超时 |
| `tools/feedback_tools.py` | 新建 | `create_feedback_tool` 工厂（闭包注入 session\_id）+ 核心生成器 |
| `tools/tool_constants.py` | 修改 | 加 `RENDER_FEEDBACK_CARD` 常量 |
| `app/routes/chat.py` | 修改 | `POST /chat/feedback` 端点；流 finally 联动 cancel |
| `app/routes/mng_proxy.py` | 修改 | AUTH\_MOCK=true 且 MNG\_INTENT\_URL 为空时读 mock JSON |
| `config/mock/presentation_custom_components.json` | 新建 | feedback\_confirm / feedback\_form 两个组件完整配置 |
| `skills/feedback_demo/SKILL.md` + `tools.py` | 新建 | 剧本技能 |
| `config/skill_config.yml` | 修改 | 注册 feedback\_demo |
| `app/services/orchestrator_service.py` | 修改 | all\_tools 挂入 feedback\_tool（一处插入） |
| `tests/test_feedback_service.py` / `test_feedback_tool.py` / `test_feedback_route.py` / `test_mng_proxy_mock.py` | 新建 | 对应单测 |

前端（goodChatAgentscope）：

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `src/components/cards/types.ts` | 修改 | `CardFeedbackPayload` 类型 |
| `src/components/cards/renderers/TemplateCardComponent.tsx` | 修改 | 表单收集约定 + onCardFeedback |
| `src/components/cards/renderers/CardRenderer.tsx` | 修改 | 透传 onCardFeedback |
| `src/services/feedback-api.ts` | 新建 | `submitCardFeedback`（纯 API，不引 store） |
| `src/hooks/use-card-feedback.ts` | 新建 | `useCardFeedback` hook（store + services + types 聚合） |
| `src/components/message/assistant-message.tsx` | 修改 | 接线 onCardFeedback |
| `src/components/message/ai-stream-renderer.tsx` | 修改 | 接线 onCardFeedback |
| `src/pages/card-test/presets.ts` | 修改 | feedback\_confirm / feedback\_form 预设 |
| `src/components/cards/renderers/TemplateCardComponent.test.tsx` | 新建 | 交互约定单测 |

---

### 后端任务 1：feedback\_service 挂起注册表

**文件：**

*   新建：`app/services/feedback_service.py`
    
*   测试：`tests/test_feedback_service.py`
    

**接口：**

*   产出：`wait_feedback(session_id: str, feedback_id: str, timeout: float) -> dict`（阻塞至 resolve/cancel/超时，返回 `{"action","payload"}` 或 `{"status","message"}`）；`resolve_feedback(session_id: str, feedback_id: str, result: dict) -> bool`；`cancel_feedback(session_id: str) -> None`。任务 2/3 消费这三个函数。
    
*   [ ] **步骤 1：写失败测试**
    

```python
"""feedback_service 挂起注册表单测。"""
import asyncio

import pytest

from app.services import feedback_service
from app.services.feedback_service import (
    cancel_feedback,
    resolve_feedback,
    wait_feedback,
)


@pytest.mark.asyncio
async def test_wait_and_resolve_returns_result():
    async def waiter():
        return await wait_feedback("sess-1", "fb-1", timeout=2)

    task = asyncio.create_task(waiter())
    await asyncio.sleep(0.05)  # 等 waiter 注册 pending
    ok = resolve_feedback("sess-1", "fb-1", {"action": "confirm", "payload": {"choice": "准入"}})
    assert ok is True
    result = await asyncio.wait_for(task, 2)
    assert result == {"action": "confirm", "payload": {"choice": "准入"}}


@pytest.mark.asyncio
async def test_wait_timeout_returns_timeout_status():
    result = await wait_feedback("sess-2", "fb-2", timeout=0.05)
    assert result["status"] == "timeout"
    assert "message" in result


@pytest.mark.asyncio
async def test_wait_cancelled_returns_cancelled_status():
    async def waiter():
        return await wait_feedback("sess-3", "fb-3", timeout=2)

    task = asyncio.create_task(waiter())
    await asyncio.sleep(0.05)
    cancel_feedback("sess-3")
    result = await asyncio.wait_for(task, 2)
    assert result["status"] == "cancelled"


@pytest.mark.asyncio
async def test_resolve_rejects_unknown_or_mismatched():
    assert resolve_feedback("no-such", "fb-x", {}) is False
    async def waiter():
        return await wait_feedback("sess-4", "fb-4", timeout=2)
    task = asyncio.create_task(waiter())
    await asyncio.sleep(0.05)
    assert resolve_feedback("sess-4", "wrong-id", {}) is False
    assert resolve_feedback("sess-4", "fb-4", {"action": "confirm", "payload": {}}) is True
    await asyncio.wait_for(task, 2)
    # wait 返回即 pop：此后重复 resolve 落到不存在分支
    assert resolve_feedback("sess-4", "fb-4", {}) is False


@pytest.mark.asyncio
async def test_second_wait_rejected_while_pending():
    async def first():
        return await wait_feedback("sess-5", "fb-5a", timeout=0.3)

    task = asyncio.create_task(first())
    await asyncio.sleep(0.05)
    second = await wait_feedback("sess-5", "fb-5b", timeout=0.1)
    assert second["status"] == "error"
    result = await asyncio.wait_for(task, 2)
    assert result["status"] == "timeout"
    assert feedback_service._pending == {}

```

*   [ ] **步骤 2：跑测试确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_feedback_service.py -v` 预期：FAIL（`ModuleNotFoundError: app.services.feedback_service`）

*   [ ] **步骤 3：实现 feedback\_service.py**
    

```python
"""卡片反馈挂起注册表。

为 render_feedback_card 工具提供同轮会话内的「发卡 → 挂起 → 回传 → 恢复」能力：
工具发卡后在此注册 pending 并 await，POST /chat/feedback 收到用户反馈后
resolve 唤醒工具，agent 在同一轮 ReAct 循环内继续推理。

按 session 单槽：同一会话流同时只允许一个挂起反馈，防止 agent 在
上一个反馈未回收时连续发卡导致反馈错配。原型阶段为进程内状态，
不做持久化（断线/重启即丢，前端点击将得到 404 并禁用卡片）。
"""
import asyncio
import logging
import os
import time
from dataclasses import dataclass, field
from typing import Any, Dict, Optional

logger = logging.getLogger(__name__)

# 挂起超时（秒），本地开发可通过环境变量覆盖（验证超时路径时可调小）
FEEDBACK_TIMEOUT_SECONDS = float(os.getenv("FEEDBACK_TIMEOUT_SECONDS", "300"))


@dataclass
class PendingFeedback:
    feedback_id: str
    session_id: str
    event: asyncio.Event = field(default_factory=asyncio.Event)
    result: Optional[Dict[str, Any]] = None
    created_at: float = field(default_factory=time.time)


_pending: Dict[str, PendingFeedback] = {}


async def wait_feedback(session_id: str, feedback_id: str,
                        timeout: Optional[float] = None) -> Dict[str, Any]:
    """注册挂起并等待用户反馈（阻塞直至 resolve/cancel/超时）。

    Returns:
        resolve 传入的 {"action","payload"}；或 {"status","message"} 形态的
        timeout / cancelled / error 结果（由 agent 据此优雅收尾）。
    """
    if timeout is None:
        timeout = FEEDBACK_TIMEOUT_SECONDS
    if session_id in _pending:
        logger.warning("[feedback_service] session=%s 已有挂起反馈，拒绝二次注册", session_id)
        return {"status": "error", "message": "上一反馈未完成，请先等待用户反馈后再发下一张卡"}
    pending = PendingFeedback(feedback_id=feedback_id, session_id=session_id)
    _pending[session_id] = pending
    logger.info("[feedback_service] 挂起等待反馈 session=%s feedback_id=%s timeout=%s",
                session_id, feedback_id, timeout)
    try:
        await asyncio.wait_for(pending.event.wait(), timeout=timeout)
        if pending.result is None:
            return {"status": "cancelled", "message": "会话已结束，反馈被取消"}
        return pending.result
    except asyncio.TimeoutError:
        logger.warning("[feedback_service] 反馈超时 session=%s feedback_id=%s", session_id, feedback_id)
        return {"status": "timeout", "message": f"用户未在 {int(timeout)} 秒内反馈"}
    finally:
        _pending.pop(session_id, None)


def resolve_feedback(session_id: str, feedback_id: str, result: Dict[str, Any]) -> bool:
    """回传端点调用：填充结果并唤醒挂起的 wait。

    Returns:
        True=已唤醒；False=不存在或 feedback_id 不匹配（404 语义）。
    """
    pending = _pending.get(session_id)
    if pending is None or pending.feedback_id != feedback_id:
        return False
    if pending.result is not None:
        return False
    pending.result = result
    pending.event.set()
    logger.info("[feedback_service] 反馈已回传 session=%s feedback_id=%s", session_id, feedback_id)
    return True


def cancel_feedback(session_id: str) -> None:
    """会话流终止（正常结束/中断/stop）时清理挂起，唤醒 wait 走 cancelled 分支。"""
    pending = _pending.pop(session_id, None)
    if pending is not None:
        logger.info("[feedback_service] 清理挂起反馈 session=%s feedback_id=%s",
                    session_id, pending.feedback_id)
        pending.event.set()

```

*   [ ] **步骤 4：跑测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_feedback_service.py -v` 预期：5 个用例通过

---

### 后端任务 2：render\_feedback\_card 工具 + orchestrator 注册

**文件：**

*   新建：`tools/feedback_tools.py`
    
*   修改：`tools/tool_constants.py`（文件末尾「Skill 管理工具」段之前插入新段）
    
*   修改：`app/services/orchestrator_service.py`（工具装配区）
    
*   测试：`tests/test_feedback_tool.py`
    

**接口：**

*   消费：任务 1 的 `wait_feedback`。
    
*   产出：`create_feedback_tool(session_id: str) -> FunctionTool`（name=`render_feedback_card`）；内部核心生成器 `_feedback_card_stream(session_id, card_type, schema, title)`，任务 2 测试直接消费。
    
*   [ ] **步骤 1：tool\_constants.py 加常量**
    

在 `tools/tool_constants.py` 的 `GET_CUSTOM_COMPONENT_CONFIG = "get_custom_component_config"` 之后插入：

```python
# ===================== 卡片反馈工具 =====================

# 卡片反馈渲染工具（发卡 + 挂起等待用户反馈，同轮恢复）
RENDER_FEEDBACK_CARD = "render_feedback_card"

```

*   [ ] **步骤 2：写失败测试**
    

```python
"""render_feedback_card 工具三段产出单测：先发卡 → 挂起 → 末次返回反馈。"""
import asyncio
import json

import pytest
from agentscope.tool import ToolChunk

from app.services.feedback_service import resolve_feedback
from tools.feedback_tools import _feedback_card_stream, create_feedback_tool
from tools.tool_constants import RENDER_FEEDBACK_CARD


def _chunk_json(chunk: ToolChunk) -> dict:
    return json.loads(chunk.content[0].text)


@pytest.mark.asyncio
async def test_stream_yields_card_then_feedback():

    chunks = [ ]


    async def consume():
        async for c in _feedback_card_stream(
            "sess-t", "feedback_confirm", {"options": [{"label": "准入", "value": "access"}]}, "请确认",
        ):
            chunks.append(c)

    task = asyncio.create_task(consume())
    await asyncio.sleep(0.05)  # 第一个 chunk 已产出，此时已挂起

    card = _chunk_json(chunks[0])
    assert card["type"] == "chart"
    assert card["chartType"] == "feedback_confirm"
    assert card["title"] == "请确认"
    feedback_id = card["schema"]["feedbackId"]
    assert feedback_id

    resolve_feedback("sess-t", feedback_id, {"action": "confirm", "payload": {"choice": "准入"}})
    await asyncio.wait_for(task, 2)

    assert _chunk_json(chunks[1]) == {"action": "confirm", "payload": {"choice": "准入"}}
    assert chunks[1].is_last is True
    assert chunks[0].is_last is not True


@pytest.mark.asyncio
async def test_stream_timeout_returns_timeout_json():

    chunks = [ ]

    async for c in _feedback_card_stream("sess-t2", "feedback_form", {}, timeout=0.05):
        chunks.append(c)
    assert _chunk_json(chunks[1])["status"] == "timeout"


def test_create_feedback_tool_named():
    tool = create_feedback_tool("sess-x")
    assert tool.name == RENDER_FEEDBACK_CARD

```

*   [ ] **步骤 3：跑测试确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_feedback_tool.py -v` 预期：FAIL（`ModuleNotFoundError: tools.feedback_tools`）

*   [ ] **步骤 4：实现 feedback\_tools.py**
    

```python
"""卡片反馈工具：发卡 → 挂起等待用户反馈 → 返回反馈供 agent 同轮继续。

对应 spec: c-harness/docs/specs/2026-09-10-卡片反馈暂停机制-design.md 4.2①。
产出格式与 render_generic_card 同构（type=chart + chartType），chat_service
的 _COMPONENT_TYPES 白名单无需改动即可被拦截转发为 CUSTOM_COMPONENT。
"""
import json
import logging
import uuid
from typing import Any, AsyncGenerator, Dict, Optional

from agentscope.message import TextBlock
from agentscope.tool import FunctionTool, ToolChunk

from app.services.feedback_service import wait_feedback
from tools.tool_constants import RENDER_FEEDBACK_CARD

logger = logging.getLogger(__name__)


def _chunk(text: str, is_last: bool = False) -> ToolChunk:
    return ToolChunk(content=[TextBlock(type="text", text=text)], is_last=is_last)


async def _feedback_card_stream(
    session_id: str,
    card_type: str,
    schema: Dict[str, Any],
    title: str = "",
    timeout: Optional[float] = None,
) -> AsyncGenerator[ToolChunk, None]:
    """核心生成器：① yield 卡片 JSON（被 chat_service 拦截为 CUSTOM_COMPONENT）
    ② 挂起等待 ③ yield 用户反馈 JSON（is_last）。

    feedbackId 塞进 schema 下发，前端回传时据此对账。
    """
    feedback_id = str(uuid.uuid4())
    payload: Dict[str, Any] = {
        "type": "chart",
        "chartType": card_type,
        "schema": {**(schema or {}), "feedbackId": feedback_id},
    }
    if title:
        payload["title"] = title
    logger.info("[feedback_tools] 发卡并挂起 session=%s card_type=%s feedback_id=%s",
                session_id, card_type, feedback_id)
    yield _chunk(json.dumps(payload, ensure_ascii=False))

    result = await wait_feedback(session_id, feedback_id, timeout=timeout)

    logger.info("[feedback_tools] 收到反馈 session=%s feedback_id=%s status=%s",
                session_id, feedback_id, result.get("status", "ok"))
    yield _chunk(json.dumps(result, ensure_ascii=False), is_last=True)


def create_feedback_tool(session_id: str) -> FunctionTool:
    """创建卡片反馈 FunctionTool（闭包注入 session_id，同 policy_qa_tools 工厂模式）。

    Args:
        session_id: 当前会话 ID（与 POST /chat/feedback 回传的 session_id 对齐）
    """
    async def render_feedback_card(
        card_type: str,
        schema: Dict[str, Any],
        title: str = "",
    ) -> AsyncGenerator[ToolChunk, None]:
        """渲染交互卡片（确认/表单）并暂停等待用户反馈，收到反馈后返回给模型继续推理。

        卡片发出后本工具会挂起直到用户在卡片上提交反馈（confirm/cancel/submit），
        超时返回 {"status":"timeout"}。

        Args:
            card_type: 组件类型标识（管理端 componentType，如 feedback_confirm / feedback_form）
            schema: 卡片业务数据（字段结构见对应组件的 schemaFields）
            title: 卡片标题
        """
        async for chunk in _feedback_card_stream(session_id, card_type, schema, title):
            yield chunk

    return FunctionTool(render_feedback_card, name=RENDER_FEEDBACK_CARD)

```

*   [ ] **步骤 5：跑测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_feedback_tool.py -v` 预期：3 个用例通过

*   [ ] **步骤 6：orchestrator\_service 注册工具**
    

`app/services/orchestrator_service.py` 工具装配区（`policy_qa_tool = create_policy_qa_tool(...)` 一行之后）插入：

```python
        # 卡片反馈工具（同轮暂停-恢复，闭包注入 session_id）
        feedback_tool = create_feedback_tool(session_id=session_id_safe)

```

同文件 import 区（`from tools.policy_qa_tools import create_policy_qa_tool` 之后）加：

```python
        from tools.feedback_tools import create_feedback_tool

```

`all_tools` 拼接处改为（原：`+ [policy_qa_tool] + md_tools`）：

```python
            create_opensandbox_tools(adapter) + _chart_tools
            + [policy_qa_tool, feedback_tool] + md_tools

```

*   [ ] **步骤 7：全量回归**
    

运行：`.venv/bin/python -m pytest tests/ -x -q` 预期：全部通过（既有 37+ 个测试文件不回归）

---

### 后端任务 3：POST /chat/feedback 端点 + 流清理联动

**文件：**

*   修改：`app/routes/chat.py`（StopRequest 类后加 FeedbackRequest；stop 路由后加端点；stream 的 finally 加一行）
    
*   测试：`tests/test_feedback_route.py`
    

**接口：**

*   消费：任务 1 的 `resolve_feedback` / `cancel_feedback`。
    
*   产出：`POST /chat/feedback`，请求体 `{session_id, feedback_id, action, payload}`，响应 `{"ok": true}` 或 404；前端任务 7 消费此契约。
    
*   [ ] **步骤 1：写失败测试**
    

```python
"""POST /chat/feedback 端点单测（httpx ASGI 传输，绕过真实网络）。"""
import asyncio

import httpx
import pytest
from fastapi import FastAPI
from httpx import ASGITransport

from app.dependencies import current_user
from app.routes.chat import router as chat_router
from app.services.feedback_service import wait_feedback


def _make_app() -> FastAPI:
    app = FastAPI()
    app.include_router(chat_router)
    app.dependency_overrides[current_user] = lambda: {"user_id": "u-test"}
    return app


@pytest.mark.asyncio
async def test_feedback_resolves_pending_wait():
    app = _make_app()
    task = asyncio.create_task(wait_feedback("sess-r1", "fb-r1", timeout=2))
    await asyncio.sleep(0.05)
    async with httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        resp = await client.post("/chat/feedback", json={
            "session_id": "sess-r1", "feedback_id": "fb-r1",
            "action": "submit", "payload": {"customerName": "测试客户"},
        })
    assert resp.status_code == 200
    assert resp.json() == {"ok": True}
    result = await asyncio.wait_for(task, 2)
    assert result == {"action": "submit", "payload": {"customerName": "测试客户"}}


@pytest.mark.asyncio
async def test_feedback_404_when_no_pending():
    app = _make_app()
    async with httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        resp = await client.post("/chat/feedback", json={
            "session_id": "no-such", "feedback_id": "fb-x",
            "action": "confirm", "payload": {},
        })
    assert resp.status_code == 404


@pytest.mark.asyncio
async def test_feedback_401_without_auth():
    app = FastAPI()
    app.include_router(chat_router)  # 不 override current_user → 走真实鉴权
    async with httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        resp = await client.post("/chat/feedback", json={
            "session_id": "s", "feedback_id": "f", "action": "confirm", "payload": {},
        })
    assert resp.status_code == 401

```

*   [ ] **步骤 2：跑测试确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_feedback_route.py -v` 预期：FAIL（404 Not Found，路由不存在）

*   [ ] **步骤 3：实现端点与清理联动**
    

`app/routes/chat.py` 顶部 import 区补：

```python
from app.services.feedback_service import cancel_feedback, resolve_feedback

```

`StopRequest` 类定义之后加：

```python
class FeedbackRequest(BaseModel):
    """卡片反馈回传：唤醒挂起中的 render_feedback_card 工具。"""
    session_id: str
    feedback_id: str
    action: str  # confirm / cancel / submit
    payload: dict = {}

```

`stop_chat` 路由之后加：

```python
@router.post("/chat/feedback")
async def submit_feedback(body: FeedbackRequest, user: dict = Depends(current_user)):
    """回传确认/表单卡片的用户反馈，唤醒同轮挂起的 render_feedback_card 工具。

    返回 404 表示反馈已失效（超时、已处理或会话已结束），前端据此禁用卡片。
    """
    ok = resolve_feedback(
        body.session_id, body.feedback_id,
        {"action": body.action, "payload": body.payload},
    )
    if not ok:
        raise HTTPException(status_code=404, detail="反馈已失效（超时、已处理或会话已结束）")
    return {"ok": True}

```

`chat` 路由内 `stream()` 的 `finally` 块（原只 pop chat\_tasks）加一行：

```python
        finally:
            # 流结束（正常/异常/中断）后清理注册表与挂起反馈
            request.app.state.chat_tasks.pop(session_id, None)
            cancel_feedback(session_id)

```

*   [ ] **步骤 4：跑测试确认通过 + 回归**
    

运行：`.venv/bin/python -m pytest tests/test_feedback_route.py tests/test_feedback_service.py tests/test_feedback_tool.py -v` 预期：全部通过

---

### 后端任务 4：mng\_proxy AUTH\_MOCK 兜底 + mock 组件配置

**文件：**

*   修改：`app/routes/mng_proxy.py`
    
*   新建：`config/mock/presentation_custom_components.json`
    
*   测试：`tests/test_mng_proxy_mock.py`
    

**接口：**

*   产出：`AUTH_MOCK=true` 且 `MNG_INTENT_URL` 为空时，`GET /api/presentation/custom-components` 返回 mock JSON（含 feedback\_confirm / feedback\_form）；`/api/presentation/cards` 返回空列表；`AUTH_MOCK=false` 时维持 500。前端任务 6/8 消费此数据结构。
    
*   [ ] **步骤 1：写失败测试**
    

```python
"""mng_proxy AUTH_MOCK 兜底单测。"""
import pytest
from fastapi import FastAPI
from httpx import ASGITransport

import app.routes.mng_proxy as mng_proxy_module
from app.dependencies import current_user
from app.routes.mng_proxy import router


def _make_app() -> FastAPI:
    app = FastAPI()
    app.include_router(router)
    app.dependency_overrides[current_user] = lambda: {"user_id": "u-test"}
    return app


@pytest.mark.asyncio
async def test_auth_mock_returns_mock_components(monkeypatch):
    monkeypatch.setattr(mng_proxy_module, "MNG_INTENT_URL", "")
    monkeypatch.setenv("AUTH_MOCK", "true")
    async with httpx.AsyncClient(transport=ASGITransport(app=_make_app()), base_url="http://t") as c:
        resp = await c.get("/api/presentation/custom-components")
    assert resp.status_code == 200
    data = resp.json()["data"]
    types = {item["componentType"] for item in data}
    assert {"feedback_confirm", "feedback_form"} <= types
    for item in data:
        assert item["renderTemplate"]
        assert item["schemaFields"]


@pytest.mark.asyncio
async def test_auth_mock_cards_returns_empty(monkeypatch):
    monkeypatch.setattr(mng_proxy_module, "MNG_INTENT_URL", "")
    monkeypatch.setenv("AUTH_MOCK", "true")
    async with httpx.AsyncClient(transport=ASGITransport(app=_make_app()), base_url="http://t") as c:
        resp = await c.get("/api/presentation/cards")
    assert resp.status_code == 200

    assert resp.json()["data"] == [ ]



@pytest.mark.asyncio
async def test_production_without_mng_returns_500(monkeypatch):
    monkeypatch.setattr(mng_proxy_module, "MNG_INTENT_URL", "")
    monkeypatch.setenv("AUTH_MOCK", "false")
    async with httpx.AsyncClient(transport=ASGITransport(app=_make_app()), base_url="http://t") as c:
        resp = await c.get("/api/presentation/custom-components")
    assert resp.status_code == 500

```

注意：测试文件头部需 `import httpx`（上面 import 区补上）。

*   [ ] **步骤 2：跑测试确认失败**
    

运行：`.venv/bin/python -m pytest tests/test_mng_proxy_mock.py -v` 预期：FAIL（当前 MNG\_INTENT\_URL 为空直接 500，第一个用例断言 200 失败）

*   [ ] **步骤 3：写 mock 组件配置文件**
    

`config/mock/presentation_custom_components.json`（结构逐字段对齐管理端契约；renderTemplate 为 JS 函数体，交互类名按新约定 `.sp-confirm-btn` / `data-form-field` / `.sp-form-submit-btn` / `.sp-form-cancel-btn`）：

```json
{
  "success": true,
  "message": null,
  "data": [
    {
      "id": 101,
      "appId": "default",
      "componentType": "feedback_confirm",
      "componentKey": "feedback_confirm",
      "cardName": "业务确认",
      "cardDesc": "用户在卡片上选择业务类型并确认，agent 同轮继续",
      "schemaFields": "{\"title\":\"FICC 业务办理\",\"subtitle\":\"FICC 场外衍生品业务 · 请选择办理类型\",\"options\":[{\"label\":\"准入与账户开立\",\"value\":\"access\"},{\"label\":\"适当性重新准入\",\"value\":\"reaccess\"}]}",

      "renderTemplate": "var items = schema.options || []; var btns = items.map(function (it, i) { return '<div class=\"sp-confirm-btn\" data-confirm-msg=\"' + (it.value || it.label) + '\" onclick=\"selectPreviewItem(' + i + ')\" style=\"display:flex;align-items:center;gap:10px;padding:14px 16px;border:1px solid #e5e7eb;border-radius:12px;cursor:pointer;margin-bottom:8px\"><span style=\"font-size:15px;font-weight:600;color:#0f172a\">' + it.label + '</span></div>'; }).join(''); return '<div style=\"border:1px solid #e5e7eb;border-radius:16px;padding:16px;background:#fff\"><div style=\"font-size:16px;font-weight:700;color:#0f172a;margin-bottom:4px\">' + (schema.title || '请确认') + '</div><div style=\"font-size:13px;color:#64748b;margin-bottom:12px\">' + (schema.subtitle || '') + '</div>' + btns + '</div>';",

      "triggerRule": "true",
      "fallbackType": "text",
      "isEnabled": 1,
      "sortOrder": 1
    },
    {
      "id": 102,
      "appId": "default",
      "componentType": "feedback_form",
      "componentKey": "feedback_form",
      "cardName": "信息填写表单",
      "cardDesc": "用户在卡片上填写表单并提交，agent 同轮继续",
      "schemaFields": "{\"title\":\"客户信息\",\"fields\":[{\"key\":\"customerName\",\"label\":\"客户名称\",\"type\":\"text\",\"placeholder\":\"请输入客户名称\"},{\"key\":\"accountType\",\"label\":\"账户类型\",\"type\":\"select\",\"options\":[\"产品户\",\"机构户\"]},{\"key\":\"contact\",\"label\":\"联系方式\",\"type\":\"text\",\"placeholder\":\"手机号或邮箱\"}]}",

      "renderTemplate": "var fields = schema.fields || []; var inputs = fields.map(function (f) { var input = f.type === 'select' ? '<select data-form-field=\"' + f.key + '\" style=\"width:100%;padding:8px 10px;border:1px solid #e5e7eb;border-radius:8px\">' + (f.options || []).map(function (o) { return '<option value=\"' + o + '\">' + o + '</option>'; }).join('') + '</select>' : '<input data-form-field=\"' + f.key + '\" type=\"text\" placeholder=\"' + (f.placeholder || '') + '\" style=\"width:100%;padding:8px 10px;border:1px solid #e5e7eb;border-radius:8px\">'; return '<label style=\"display:block;margin-bottom:10px\"><span style=\"display:block;font-size:12px;color:#64748b;margin-bottom:4px\">' + f.label + '</span>' + input + '</label>'; }).join(''); return '<div style=\"border:1px solid #e5e7eb;border-radius:16px;padding:16px;background:#fff\"><div style=\"font-size:16px;font-weight:700;color:#0f172a;margin-bottom:12px\">' + (schema.title || '请填写') + '</div>' + inputs + '<div style=\"display:flex;gap:8px;margin-top:12px\"><button class=\"sp-form-submit-btn\" style=\"flex:1;padding:10px;background:#2563eb;color:#fff;border:none;border-radius:10px;font-weight:600;cursor:pointer\">提交</button><button class=\"sp-form-cancel-btn\" style=\"flex:1;padding:10px;background:#f1f5f9;color:#475569;border:none;border-radius:10px;font-weight:600;cursor:pointer\">取消</button></div></div>';",

      "triggerRule": "true",
      "fallbackType": "text",
      "isEnabled": 1,
      "sortOrder": 2
    }
  ]
}

```

*   [ ] **步骤 4：改 mng\_proxy.py**
    

文件头部 import 区补：

```python
import json
import os
from pathlib import Path

```

`router = APIRouter()` 之后加：

```python
# 本地 mock 组件配置目录（AUTH_MOCK=true 且 MNG_INTENT_URL 为空时生效）
_MOCK_DIR = Path(__file__).resolve().parents[2] / "config" / "mock"


def _is_auth_mock() -> bool:
    """与 user_dao 的 AUTH_MOCK 判定保持同一模式。"""
    return os.getenv("AUTH_MOCK", "true").lower() == "true"


def _load_mock_response(filename: str) -> dict:
    """读取本地 mock 组件配置；文件不存在时返回空列表封包。"""
    mock_file = _MOCK_DIR / filename
    if not mock_file.exists():

        return {"success": True, "message": None, "data": []}

    return json.loads(mock_file.read_text(encoding="utf-8"))

```

两个端点的 `if not MNG_INTENT_URL:` 分支改为（两个端点同构，仅文件名不同）：

```python
    if not MNG_INTENT_URL:
        # 本地开发兜底：仅 AUTH_MOCK=true 时读 mock；生产（AUTH_MOCK=false）维持 500
        if _is_auth_mock():
            return _load_mock_response("presentation_custom_components.json")
        raise HTTPException(status_code=500, detail="MNG_INTENT_URL not configured")

```

（`proxy_card_configs` 端点对应读 `"presentation_cards.json"`——该文件不创建，落入"文件不存在返回空列表"分支。）

*   [ ] **步骤 5：跑测试确认通过**
    

运行：`.venv/bin/python -m pytest tests/test_mng_proxy_mock.py -v` 预期：3 个用例通过

---

### 前端任务 5：前端表单交互约定 + onCardFeedback 透传

**文件：**

*   修改：`src/components/cards/types.ts`（文件末尾追加类型）
    
*   修改：`src/components/cards/renderers/TemplateCardComponent.tsx`
    
*   修改：`src/components/cards/renderers/CardRenderer.tsx`（props 与 TemplateCardComponent 调用处）
    
*   测试：`src/components/cards/renderers/TemplateCardComponent.test.tsx`
    

**接口：**

*   产出：`CardFeedbackPayload { feedbackId: string; action: "confirm" | "cancel" | "submit"; payload: Record<string, unknown> }`（`src/components/cards/types.ts` 导出）；`TemplateCardComponentProps` 与 `CardRenderer` props 新增可选 `onCardFeedback?: (feedback: CardFeedbackPayload) => void`。任务 7/8 消费。
    
*   [ ] **步骤 1：types.ts 追加类型**
    

```typescript
/** 卡片反馈回传载荷：经 POST /chat/feedback 唤醒后端挂起中的 render_feedback_card */
export interface CardFeedbackPayload {
  feedbackId: string;
  action: "confirm" | "cancel" | "submit";
  payload: Record<string, unknown>;
}

```

*   [ ] **步骤 2：写失败测试**
    

`src/components/cards/renderers/TemplateCardComponent.test.tsx`：

```typescript
import { fireEvent, render } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { TemplateCardComponent } from "@/components/cards/renderers/TemplateCardComponent";

const CONFIRM_TEMPLATE = `

var items = schema.options || [];

var btns = items.map(function (it) {
  return '<div class="sp-confirm-btn" data-confirm-msg="' + it.value + '">' + it.label + '</div>';
}).join('');
return '<div>' + btns + '</div>';
`.trim();

const FORM_TEMPLATE = `

var fields = schema.fields || [];

var inputs = fields.map(function (f) {
  return '<input data-form-field="' + f.key + '" type="text">';
}).join('');
return '<div>' + inputs
  + '<button class="sp-form-submit-btn">提交</button>'
  + '<button class="sp-form-cancel-btn">取消</button></div>';
`.trim();

function makeComponent(schema: Record<string, unknown>) {
  return { type: "chart", chartType: "feedback_confirm", schema, title: "t" };
}

describe("TemplateCardComponent feedback", () => {
  it("confirm 按钮经 data-confirm-msg 归一为 confirm action", () => {
    const onCardFeedback = vi.fn();
    const { container } = render(
      <TemplateCardComponent
        surfaceId="test"
        component={makeComponent({
          feedbackId: "fb-1",
          options: [{ label: "准入与账户开立", value: "access" }],
        })}
        renderTemplate={CONFIRM_TEMPLATE}
        onCardFeedback={onCardFeedback}
      />,
    );
    fireEvent.click(container.querySelector(".sp-confirm-btn")!);
    expect(onCardFeedback).toHaveBeenCalledWith({
      feedbackId: "fb-1",
      action: "confirm",
      payload: { message: "access" },
    });
  });

  it("表单提交收集 data-form-field 值为 submit action", () => {
    const onCardFeedback = vi.fn();
    const { container } = render(
      <TemplateCardComponent
        surfaceId="test"
        component={makeComponent({
          feedbackId: "fb-2",
          fields: [{ key: "customerName", label: "客户名称", type: "text" }],
        })}
        renderTemplate={FORM_TEMPLATE}
        onCardFeedback={onCardFeedback}
      />,
    );
    const input = container.querySelector<HTMLInputElement>('[data-form-field="customerName"]')!;
    fireEvent.change(input, { target: { value: "测试客户" } });
    fireEvent.click(container.querySelector(".sp-form-submit-btn")!);
    expect(onCardFeedback).toHaveBeenCalledWith({
      feedbackId: "fb-2",
      action: "submit",
      payload: { customerName: "测试客户" },
    });
  });

  it("取消按钮触发 cancel action；无 feedbackId 不触发回传", () => {
    const onCardFeedback = vi.fn();
    const { container } = render(
      <TemplateCardComponent
        surfaceId="test"

        component={makeComponent({ feedbackId: "fb-3", fields: [ ] })}

        renderTemplate={FORM_TEMPLATE}
        onCardFeedback={onCardFeedback}
      />,
    );
    fireEvent.click(container.querySelector(".sp-form-cancel-btn")!);
    expect(onCardFeedback).toHaveBeenCalledWith({
      feedbackId: "fb-3",
      action: "cancel",
      payload: {},
    });

    const noId = render(
      <TemplateCardComponent
        surfaceId="test2"
        component={makeComponent({ options: [{ label: "x", value: "y" }] })}
        renderTemplate={CONFIRM_TEMPLATE}
        onCardFeedback={onCardFeedback}
      />,
    );
    fireEvent.click(noId.container.querySelector(".sp-confirm-btn")!);
    expect(onCardFeedback).toHaveBeenCalledTimes(1); // 无 feedbackId 的点击被忽略
  });
});

```

*   [ ] **步骤 3：跑测试确认失败**
    

运行：`npx vitest run src/components/cards/renderers/TemplateCardComponent.test.tsx` 预期：FAIL（`onCardFeedback` prop 不存在，回调未被调用）

*   [ ] **步骤 4：改 TemplateCardComponent.tsx**
    

改动点（保持既有 `new Function` 渲染路径不动，只加事件绑定与收集逻辑）：

1.  import 与 props：
    
    ```typescript
    import type { CardFeedbackPayload } from "@/components/cards/types";
    
    interface TemplateCardComponentProps {
      surfaceId: string;
      component: Record<string, unknown>;
      renderTemplate: string;
      schemaFields?: string;
      onConfirm?: (text: string) => void;
      onCardFeedback?: (feedback: CardFeedbackPayload) => void;
    }
    
    ```
    
2.  组件内加 feedbackId 提取与派发辅助（`injectHelpers` 之后）：
    
    ```typescript
      // feedbackId 位于 component.schema（chart 类型原样透传）或 component 顶层（防御两种形态）
      const schemaObj = (component.schema && typeof component.schema === "object"
        ? component.schema
        : {}) as Record<string, unknown>;
      const feedbackId = String(schemaObj.feedbackId ?? component.feedbackId ?? "");
    
      const dispatchFeedback = (action: CardFeedbackPayload["action"], payload: Record<string, unknown>) => {
        if (!onCardFeedback || !feedbackId) return;
        onCardFeedback({ feedbackId, action, payload });
        // 乐观禁用容器内全部反馈按钮，防重复提交；后端 404 幂等兜底
        containerRef.current?.querySelectorAll(".sp-confirm-btn, .sp-form-submit-btn, .sp-form-cancel-btn")
          .forEach((btn) => ((btn as HTMLButtonElement).disabled = true));
      };
    
    ```
    
3.  `useEffect` 内、既有 `.sp-confirm-btn` 绑定之前加表单约定绑定，并把确认按钮绑定改为优先走结构化回调：
    

```typescript
      // 表单约定：收集 data-form-field 值 → submit；sp-form-cancel-btn → cancel
      const collectFormData = () => {
        const data: Record<string, unknown> = {};
        containerRef.current?.querySelectorAll<HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement>(
          "[data-form-field]",
        ).forEach((el) => {
          data[el.getAttribute("data-form-field") || ""] = el.value;
        });
        return data;
      };
      containerRef.current.querySelectorAll(".sp-form-submit-btn").forEach((btn) => {
        btn.addEventListener("click", (e) => {
          e.stopPropagation();
          dispatchFeedback("submit", collectFormData());
        });
      });
      containerRef.current.querySelectorAll(".sp-form-cancel-btn").forEach((btn) => {
        btn.addEventListener("click", (e) => {
          e.stopPropagation();
          dispatchFeedback("cancel", {});
        });
      });

```

既有 `.sp-confirm-btn` 监听器回调开头插入结构化分支（原 onConfirm 逻辑保留在 else 路径）：

```typescript
        btn.addEventListener("click", (e) => {
          e.stopPropagation();
          const dataMsg = (btn as HTMLElement).getAttribute("data-confirm-msg");
          if (onCardFeedback && feedbackId) {
            dispatchFeedback("confirm", { message: dataMsg || "" });
            return;
          }
          // —— 以下为既有 onConfirm 兼容路径，保持不动 ——
          if (dataMsg) { onConfirm?.(dataMsg); return; }
          ...（原有代码不变）
        });

```

同时把 `dispatchFeedback`/`feedbackId` 加入该 useEffect 的依赖数组（`[component, renderTemplate, schemaFields, injectHelpers, onConfirm, onCardFeedback]`）。

*   [ ] **步骤 5：CardRenderer.tsx 透传**
    

`CardRendererProps` 加 `onCardFeedback?: (feedback: CardFeedbackPayload) => void;`（import 处补类型导入），`TemplateCardComponent` 调用处（`renderTemplate 优先` 分支）加一行：

```tsx
        onCardFeedback={onCardFeedback}

```

*   [ ] **步骤 6：跑测试确认通过 + 前端回归**
    

运行：`npx vitest run` 预期：新增 3 个用例通过，既有测试无回归

---

### 前端任务 6：feedback-api + 聊天流两处接线

**文件：**

*   新建：`src/services/feedback-api.ts`
    
*   新建：`src/hooks/use-card-feedback.ts`
    
*   修改：`src/components/message/assistant-message.tsx`（CardRenderer 调用处）
    
*   修改：`src/components/message/ai-stream-renderer.tsx`（两处 CardRenderer 调用处）
    

**接口：**

*   消费：任务 3 的 `POST /chat/feedback` 契约；任务 6 的 `CardFeedbackPayload`。
    
*   产出：`submitCardFeedback(body: CardFeedbackRequest): Promise<void>`（services 层，纯 API）；`useCardFeedback()` hook（hooks 层，两处渲染点共用）。分层规约：services 不引 store/components，hook 依赖 services + store + types 属标准方向。
    
*   [ ] **步骤 1：写 feedback-api.ts（同 action-audit-api 模式）**
    

```typescript
import { API_BASE_URL } from "@/services/api-config";
import { request } from "@/services/http";

const FEEDBACK_PATH = "/chat/feedback";

/** 卡片反馈回传请求体（对齐后端 FeedbackRequest） */
export interface CardFeedbackRequest {
  session_id: string;
  feedback_id: string;
  action: "confirm" | "cancel" | "submit";
  payload: Record<string, unknown>;
}

/** 回传确认/表单卡片反馈，唤醒后端挂起中的 render_feedback_card 工具 */
export async function submitCardFeedback(body: CardFeedbackRequest): Promise<void> {
  await request<unknown>(FEEDBACK_PATH, {
    method: "POST",
    body,
    baseUrl: API_BASE_URL,
    headers: { "Content-Type": "application/json" },
  });
}

```

*   [ ] **步骤 2：写共用 hook（hooks 层）**
    

`src/hooks/use-card-feedback.ts`（新文件；hook 依赖 services + store + types，符合分层方向 `types/lib → store/services → hooks → components`）：

```typescript
import { useCallback } from "react";

import type { CardFeedbackPayload } from "@/components/cards/types";
import { useChatStore } from "@/store/chat-store";
import { useToastStore } from "@/store/toast-store";
import { submitCardFeedback } from "@/services/feedback-api";

/** 聊天流卡片反馈接线：取当前会话 ID 回传，成功/失败 toast 反馈 */
export function useCardFeedback() {
  return useCallback(async (feedback: CardFeedbackPayload) => {
    const sessionId = useChatStore.getState().currentConversationId;
    if (!sessionId || !feedback.feedbackId) return;
    try {
      await submitCardFeedback({
        session_id: sessionId,
        feedback_id: feedback.feedbackId,
        action: feedback.action,
        payload: feedback.payload,
      });
      useToastStore.getState().success("已提交");
    } catch (e) {
      const msg = e instanceof Error ? e.message : "反馈提交失败";
      useToastStore.getState().error(`反馈已失效：${msg}`);
    }

  }, []);

}

```

*   [ ] **步骤 3：两处渲染点接线**
    

`src/components/message/assistant-message.tsx`：import `useCardFeedback`（来自 `@/hooks/use-card-feedback`），组件体内 `const handleCardFeedback = useCardFeedback();`，历史消息 CardRenderer 调用处（约 L158）改为：

```tsx
<CardRenderer key={`${message.id}-comp-${index}`} payload={comp} onCardFeedback={handleCardFeedback} />

```

`src/components/message/ai-stream-renderer.tsx`：同样 import 并在组件内取 hook，两处 CardRenderer 调用（约 L346 与 L405）加 `onCardFeedback={handleCardFeedback}`。

*   [ ] **步骤 4：类型检查 + 回归**
    

运行：`npx tsc -b --noEmit && npx vitest run` 预期：无类型错误，测试全绿

---

## 风险点

*   裸触发词经意图路由不稳定（skills 注入路径 100% 稳定）——意图路由层优化属后续独立工作；
    
*   div 类确认按钮乐观禁用无效（后端 404 幂等兜底）——可加 pointer-events 或容器标记。