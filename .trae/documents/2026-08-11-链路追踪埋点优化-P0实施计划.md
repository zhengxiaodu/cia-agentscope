# 链路追踪与埋点优化 P0 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use ch-subagent-driven-development (recommended) or ch-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 cdcc-cia 链路追踪的数据正确性——trace_id 全链路对齐、结构化日志落盘、埋点基础设施就位，使用户反馈数据从"100% 无效"变为可用。

**Architecture:** 新增 `app/utils/` 下 5 个纯基础设施模块（trace_context / trace_names / logging_setup / traced / metrics-stub），扩展 `langfuse_service` 的 start/end_observation 参数，修改 `chat_service` 的 trace_ready 下发时机（流尾→流首），前端注册 `trace_ready` 事件消费器并移除自造 traceId 兜底。所有观测代码延续"非强依赖"约定：未配置时短路为空操作，异常全兜底。

**Tech Stack:** Python 3.12 / FastAPI / Langfuse 4.14.1 / AgentScope 2.0.5 / OpenTelemetry（传递依赖）/ pytest（本次新增）；React + TypeScript + Vite / vitest

## Global Constraints

- **严禁自动提交 git**：文件变动不做 git 提交，由用户手动提交。计划中的"提交检查点"步骤仅列出本次改动清单供用户审查，不执行 `git add` / `git commit`。
- **langfuse 版本**：`requirements.txt:19` 由 `langfuse>=3.5.0` 改为 `langfuse>=4.14,<5`（本方案依赖 4.x 的 `as_type="generation"` / `usage_details` 行为）。
- **fin-x-scope / fin-ai-native-mng 目录仅作只读参考**，严禁修改其中内容。
- **前端编码规范**（AGENTS.md）：变量/函数小驼峰、组件大驼峰、常量全大写下划线、严格 TS 禁 any、所有接口入参返回值必须定义明确类型。
- **后端 venv 路径**：`.venv/`（项目根目录），Python 3.12。
- **后端项目根目录**：`cdcc-cia-trae-agent/`，本计划默认在此目录下执行后端命令（`.venv/bin/python`、`.venv/bin/pip` 均相对于此目录）。
- **前端项目根目录**：`../goodChatAgentscope`（从后端根目录出发），前端命令用 `cd ../goodChatAgentscope && ...` 切换。
- **观测失败绝不影响业务**：所有新增观测代码必须 try/except 兜底 + 未配置时短路为空操作。
- **本计划仅覆盖 P0（数据正确性）**，P1（核心链路可见）与 P2（基础设施与叶子节点）为后续计划。

---

## File Structure

### 新建文件（后端）

| 文件 | 职责 |
|---|---|
| `app/utils/trace_context.py` | 请求级观测上下文：session_id/user_id 用 contextvars 存储，trace_id/span_id 从 OTel 当前 span 读取 |
| `app/utils/trace_names.py` | 埋点名称集中枚举，禁止业务代码写字符串字面量 |
| `app/utils/logging_setup.py` | JSON 结构化日志：TraceJsonFormatter + setup_logging() |
| `app/utils/traced.py` | 统一埋点包装器：traced_stage() context manager，一次调用同时产出 observation + 日志 |
| `app/utils/metrics.py` | 指标 stub（P2 替换为真实 Prometheus 实现） |
| `tests/__init__.py` | 测试包 |
| `tests/conftest.py` | pytest 公共 fixture |
| `tests/test_trace_context.py` | trace_context 单元测试 |
| `tests/test_trace_names.py` | trace_names 单元测试 |
| `tests/test_logging_setup.py` | logging_setup 单元测试 |
| `tests/test_langfuse_service_extension.py` | langfuse_service 扩展参数测试 |
| `tests/test_traced.py` | traced_stage 包装器测试 |
| `tests/test_trace_ready.py` | trace_ready 早期下发 + _finalize_trace 测试 |

### 修改文件（后端）

| 文件 | 改动 |
|---|---|
| `requirements.txt` | pin langfuse>=4.14,<5；新增 pytest + pytest-asyncio |
| `app/config.py` | 增 LOG_LEVEL / LOG_DIR / LOG_MAX_BYTES / LOG_BACKUP_COUNT |
| `app/main.py` | lifespan 首行调 setup_logging()；print()→logger.info；shutdown 补 flush()；注册 get_current_langfuse 单例 |
| `app/services/langfuse_service.py` | start_observation 增 model/metadata 参数；end_observation 增 usage_details/level/status_message 参数；新增 get_current_langfuse() 模块级访问器 |
| `app/services/chat_service.py` | 根 span 内立即下发 trace_ready（流首）；_finalize_trace 增 already_emitted 参数避免重复下发 |
| `.env.example` | 增 LOG_LEVEL / LOG_DIR / LOG_MAX_BYTES / LOG_BACKUP_COUNT |
| `docker-compose.yml` | 挂载 `./logs:/app/logs` 卷 |

### 修改文件（前端）

| 文件 | 改动 |
|---|---|
| `src/types/ai-stream.ts` | AiStreamEventType 增 `"TRACE_READY"`；AiStreamEvent 联合类型增 `TraceReadyEvent` |
| `src/services/ai-stream/event-parser.ts` | eventNormalizerRegistry 增 `trace_ready` normalizer |
| `src/services/ai-stream/event-parser.test.ts` | 增 trace_ready 测试用例 |
| `src/hooks/use-chat-stream.ts` | 删 L91 `traceId: createTraceId()`；handleNormalizedEvent 增 TRACE_READY 处理 |
| `src/services/sessions-api.ts` | 删 L89 `createTraceId()` 兜底 |

---

### Task 1: 后端测试基建 + 版本 pin

**Files:**
- Modify: `requirements.txt:19`
- Create: `tests/__init__.py`, `tests/conftest.py`

**Interfaces:**
- Produces: `pytest` 可执行、`pytest-asyncio` 已安装、`tests/` 目录就绪

- [ ] **Step 1: 修改 requirements.txt**

在 `requirements.txt` 末尾追加测试依赖，并修改 langfuse 版本约束：

```
# 末尾追加：
pytest>=8.0
pytest-asyncio>=0.23
```

同时将 L19 的 `langfuse>=3.5.0` 改为 `langfuse>=4.14,<5`。

- [ ] **Step 2: 安装新依赖**

Run: `.venv/bin/pip install -r requirements.txt`
Expected: 成功安装 pytest 和 pytest-asyncio

- [ ] **Step 3: 创建 tests/ 目录和 conftest.py**

`tests/__init__.py`（空文件）。

`tests/conftest.py`：

```python
"""pytest 公共配置。"""
import pytest

# pytest-asyncio 配置：自动为 async test 添加 event loop
pytest_plugins = ("pytest_asyncio",)


def pytest_collection_modifyitems(items):
    """为所有 async def test_ 自动标记 asyncio 标签。"""
    for item in items:
        if asyncio_marker_needed(item):
            item.add_marker(pytest.mark.asyncio)


def asyncio_marker_needed(item) -> bool:
    """判断一个 test item 是否需要 asyncio 标记。"""
    import inspect
    if not inspect.iscoroutinefunction(item.function):
        return False
    return not any(m.name == "asyncio" for m in item.own_markers)
```

- [ ] **Step 4: 验证 pytest 可运行**

Run: `.venv/bin/python -m pytest --version`
Expected: 输出 pytest 版本号

Run: `.venv/bin/python -m pytest tests/ -v --tb=short`
Expected: "no tests ran"（tests/ 目录还没有测试文件，但不报错）

- [ ] **Step 5: 提交检查点**

本次改动清单（供用户审查，不自动提交）：
- `requirements.txt`：pin langfuse>=4.14,<5；新增 pytest + pytest-asyncio
- `tests/__init__.py`、`tests/conftest.py`：新建

---

### Task 2: P0-1 trace_context.py

**Files:**
- Create: `app/utils/trace_context.py`
- Test: `tests/test_trace_context.py`

**Interfaces:**
- Produces: `set_request_context(session_id, user_id) -> None`、`clear_request_context() -> None`、`get_session_id() -> Optional[str]`、`get_user_id() -> Optional[str]`、`get_trace_id() -> Optional[str]`、`get_span_id() -> Optional[str]`

- [ ] **Step 1: 写失败测试**

`tests/test_trace_context.py`：

```python
"""trace_context 单元测试。"""
from app.utils.trace_context import (
    set_request_context,
    clear_request_context,
    get_session_id,
    get_user_id,
    get_trace_id,
    get_span_id,
)


def test_session_id_set_and_get():
    set_request_context("sess-123", "user-456")
    assert get_session_id() == "sess-123"
    assert get_user_id() == "user-456"


def test_clear_request_context():
    set_request_context("sess-123", "user-456")
    clear_request_context()
    assert get_session_id() is None
    assert get_user_id() is None


def test_get_trace_id_returns_none_without_span():
    """无活跃 OTel span 时返回 None。"""
    assert get_trace_id() is None


def test_get_span_id_returns_none_without_span():
    assert get_span_id() is None


def test_set_request_context_ignores_none():
    """传 None 不覆盖已有值。"""
    set_request_context("sess-1", "user-1")
    set_request_context(None, None)
    assert get_session_id() == "sess-1"
    assert get_user_id() == "user-1"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_trace_context.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.utils.trace_context'`

- [ ] **Step 3: 实现 trace_context.py**

`app/utils/trace_context.py`（代码取自优化方案 P0-1，见方案文档 L181-246）：

```python
"""链路追踪上下文：请求级观测元信息的隐式传递。

设计要点：
- trace_id / span_id 不自行生成、不落 contextvars，统一从 OpenTelemetry
  当前 span 读取。langfuse 4.x 基于 OTel 构建，其 trace_id 与 OTel
  span_context.trace_id 同源（已实测一致），因此日志与 Langfuse trace
  天然可双向跳转，无需把 langfuse_service 逐层透传。
- session_id / user_id 在请求入口注入一次，全链路就地读取。
"""
import contextvars
from typing import Optional

from opentelemetry import trace as otel_trace

_session_id: contextvars.ContextVar[Optional[str]] = contextvars.ContextVar(
    "trace_session_id", default=None
)
_user_id: contextvars.ContextVar[Optional[str]] = contextvars.ContextVar(
    "trace_user_id", default=None
)


def set_request_context(session_id: Optional[str], user_id: Optional[str]) -> None:
    """请求入口注入业务上下文。"""
    if session_id:
        _session_id.set(session_id)
    if user_id:
        _user_id.set(user_id)


def clear_request_context() -> None:
    """请求结束清理，避免异步上下文串味。"""
    _session_id.set(None)
    _user_id.set(None)


def get_session_id() -> Optional[str]:
    return _session_id.get()


def get_user_id() -> Optional[str]:
    return _user_id.get()


def get_trace_id() -> Optional[str]:
    """取当前 trace_id（32 位 hex），与 Langfuse observation.trace_id 一致。"""
    try:
        ctx = otel_trace.get_current_span().get_span_context()
        if ctx and ctx.is_valid:
            return format(ctx.trace_id, "032x")
    except Exception:
        pass
    return None


def get_span_id() -> Optional[str]:
    """取当前 span_id（16 位 hex）。"""
    try:
        ctx = otel_trace.get_current_span().get_span_context()
        if ctx and ctx.is_valid:
            return format(ctx.span_id, "016x")
    except Exception:
        pass
    return None
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_trace_context.py -v`
Expected: 5 passed

- [ ] **Step 5: 提交检查点**

改动清单：`app/utils/trace_context.py`（新建）、`tests/test_trace_context.py`（新建）

---

### Task 3: P0-5 trace_names.py

**Files:**
- Create: `app/utils/trace_names.py`
- Test: `tests/test_trace_names.py`

**Interfaces:**
- Produces: `TraceName`（StrEnum），含 `CHAT_RESPONSE`、`REACT_STEP`、`AGENT_RUN`、`LLM_QUERY_REWRITE` 等成员

- [ ] **Step 1: 写失败测试**

`tests/test_trace_names.py`：

```python
"""trace_names 单元测试。"""
from app.utils.trace_names import TraceName


def test_chat_response_value():
    assert TraceName.CHAT_RESPONSE == "chat-response"


def test_agent_run_format():
    assert TraceName.AGENT_RUN.format(agent_id="general") == "agent-general"


def test_react_step_format():
    assert TraceName.REACT_STEP.format(step=3) == "react-step-3"


def test_tool_call_format():
    assert TraceName.TOOL_CALL.format(tool_name="Bash") == "tool-Bash"


def test_all_values_are_strings():
    for name in TraceName:
        assert isinstance(name.value, str)
        assert "-" in name.value or name.value.count("-") >= 0  # kebab-case
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_trace_names.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 实现 trace_names.py**

`app/utils/trace_names.py`（代码取自优化方案第五章，见方案文档 L817-861）：

```python
"""埋点名称集中定义。禁止在业务代码中写字符串字面量。

命名约定：
- 全小写 kebab-case，层级用 '-' 分隔
- LLM 调用统一 llm-* 前缀，工具调用统一 tool-* 前缀
- 含动态部分的用 {} 占位，通过 .format() 生成
"""
from enum import StrEnum


class TraceName(StrEnum):
    # ---- 会话主链路 ----
    CHAT_RESPONSE = "chat-response"
    HISTORY_LOAD = "history-load"
    WORKSPACE_LOAD = "workspace-load"
    WORKSPACE_INIT = "workspace-initialize"
    SKILL_LOAD = "skill-load"

    # ---- 意图链路 ----
    QUERY_REWRITE = "query-rewrite"
    INTENT_RECOGNITION = "intent-recognition"
    INTENT_ORCHESTRATION = "intent-orchestration"

    # ---- 执行层 ----
    AGENT_RUN = "agent-{agent_id}"
    REACT_STEP = "react-step-{step}"

    # ---- LLM（generation 类型）----
    LLM_QUERY_REWRITE = "llm-query-rewrite"
    LLM_INTENT_RECOGNITION = "llm-intent-recognition"
    LLM_INTENT_ORCHESTRATION = "llm-intent-orchestration"
    LLM_REACT_THINK = "llm-react-think"
    LLM_RECOMMENDED_QUESTIONS = "llm-recommended-questions"
    LLM_AGENT_CALL = "llm-{model_name}"

    # ---- 工具（tool 类型）----
    TOOL_CALL = "tool-{tool_name}"

    # ---- 收尾与外部依赖 ----
    RECOMMENDED_QUESTIONS = "recommended-questions"
    PERSIST_HISTORY = "persist-history"
    FILE_DETECT = "file-detect"
    HTTP_CALL = "http-{target}"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_trace_names.py -v`
Expected: 5 passed

- [ ] **Step 5: 提交检查点**

改动清单：`app/utils/trace_names.py`（新建）、`tests/test_trace_names.py`（新建）

---

### Task 4: P0-2 logging_setup.py + config 接线 + P0-4 flush

**Files:**
- Create: `app/utils/logging_setup.py`
- Modify: `app/config.py`（L91 后追加 LOG_* 变量）
- Modify: `app/main.py`（lifespan 首行 + print→logger + shutdown flush）
- Modify: `.env.example`（L33 后追加 LOG_* 变量）
- Modify: `docker-compose.yml`（volumes 段增 logs 映射）
- Test: `tests/test_logging_setup.py`

**Interfaces:**
- Consumes: `app.utils.trace_context.get_trace_id` / `get_session_id` / `get_user_id` / `get_span_id`
- Produces: `TraceJsonFormatter`（logging.Formatter 子类）、`setup_logging() -> None`

- [ ] **Step 1: 写失败测试**

`tests/test_logging_setup.py`：

```python
"""logging_setup 单元测试。"""
import json
import logging

from app.utils.logging_setup import TraceJsonFormatter
from app.utils.trace_context import set_request_context, clear_request_context


def _make_record(msg="hello %s", args=("world",), extra=None):
    record = logging.LogRecord(
        name="test.logger", level=logging.INFO, pathname="/t.py",
        lineno=1, msg=msg, args=args, exc_info=None,
    )
    if extra:
        for k, v in extra.items():
            setattr(record, k, v)
    return record


def test_formatter_outputs_valid_json():
    formatter = TraceJsonFormatter()
    output = formatter.format(_make_record())
    data = json.loads(output)
    assert data["message"] == "hello world"
    assert data["level"] == "INFO"
    assert data["logger"] == "test.logger"
    assert "@timestamp" in data


def test_formatter_includes_session_id_when_set():
    set_request_context("sess-test", "user-test")
    try:
        formatter = TraceJsonFormatter()
        output = formatter.format(_make_record())
        data = json.loads(output)
        assert data["sessionId"] == "sess-test"
        assert data["userId"] == "user-test"
    finally:
        clear_request_context()


def test_formatter_includes_biz_extra():
    formatter = TraceJsonFormatter()
    record = _make_record(extra={"biz": {"stage": "query-rewrite", "durationMs": 42}})
    output = formatter.format(record)
    data = json.loads(output)
    assert data["biz"]["stage"] == "query-rewrite"
    assert data["biz"]["durationMs"] == 42


def test_formatter_omits_trace_fields_when_no_context():
    formatter = TraceJsonFormatter()
    output = formatter.format(_make_record())
    data = json.loads(output)
    assert "traceId" not in data
    assert "sessionId" not in data
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_logging_setup.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 实现 logging_setup.py**

`app/utils/logging_setup.py`（代码取自优化方案 P0-2，见方案文档 L252-337）：

```python
"""日志初始化：JSON 结构化输出 + trace 上下文自动注入 + 级别可配。

解决三件事：
1. 无 basicConfig 导致默认 WARNING+ 生效，线上所有 logger.info 静默丢弃；
2. 日志无 trace_id，无法与 Langfuse trace 互跳，并发下多条日志无法归组；
3. 无 JSON schema、无落盘，无法被日志系统采集聚合。
"""
import json
import logging
import logging.handlers
import os
import sys
from datetime import datetime, timezone

from app.utils.trace_context import (
    get_session_id, get_span_id, get_trace_id, get_user_id,
)


class TraceJsonFormatter(logging.Formatter):
    """一行一个 JSON，自动注入 trace 上下文字段。"""

    def format(self, record: logging.LogRecord) -> str:
        payload = {
            "@timestamp": datetime.fromtimestamp(
                record.created, tz=timezone.utc
            ).isoformat(timespec="milliseconds"),
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
        }
        for key, value in (
            ("traceId", get_trace_id()),
            ("spanId", get_span_id()),
            ("sessionId", get_session_id()),
            ("userId", get_user_id()),
        ):
            if value:
                payload[key] = value
        biz = getattr(record, "biz", None)
        if isinstance(biz, dict):
            payload["biz"] = biz
        if record.exc_info:
            payload["stackTrace"] = self.formatException(record.exc_info)
        try:
            return json.dumps(payload, ensure_ascii=False, separators=(",", ":"))
        except Exception:
            payload["biz"] = str(biz)
            return json.dumps(payload, ensure_ascii=False, default=str)


def setup_logging() -> None:
    """在 lifespan 最开始调用。控制台保留可读文本，文件输出 JSON。"""
    level = getattr(logging, os.getenv("LOG_LEVEL", "INFO").upper(), logging.INFO)
    root = logging.getLogger()
    root.setLevel(level)
    for handler in list(root.handlers):
        root.removeHandler(handler)

    console = logging.StreamHandler(sys.stdout)
    console.setFormatter(logging.Formatter(
        "%(asctime)s %(levelname)s [%(name)s] %(message)s"
    ))
    root.addHandler(console)

    log_dir = os.getenv("LOG_DIR", "logs")
    try:
        os.makedirs(log_dir, exist_ok=True)
        file_handler = logging.handlers.RotatingFileHandler(
            os.path.join(log_dir, "observability.json"),
            maxBytes=int(os.getenv("LOG_MAX_BYTES", str(64 * 1024 * 1024))),
            backupCount=int(os.getenv("LOG_BACKUP_COUNT", "7")),
            encoding="utf-8",
        )
        file_handler.setFormatter(TraceJsonFormatter())
        root.addHandler(file_handler)
    except Exception:
        root.warning("JSON 日志文件初始化失败，仅保留控制台输出", exc_info=True)

    for noisy in ("httpx", "httpcore", "urllib3", "docker"):
        logging.getLogger(noisy).setLevel(logging.WARNING)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_logging_setup.py -v`
Expected: 4 passed

- [ ] **Step 5: 修改 config.py 追加 LOG_* 变量**

在 `app/config.py` 的 `LANGFUSE_HOST` 行（L65）之后追加：

```python
# 日志配置
LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO")
LOG_DIR = os.getenv("LOG_DIR", "logs")
LOG_MAX_BYTES = int(os.getenv("LOG_MAX_BYTES", str(64 * 1024 * 1024)))
LOG_BACKUP_COUNT = int(os.getenv("LOG_BACKUP_COUNT", "7"))
```

- [ ] **Step 6: 修改 .env.example 追加 LOG_* 变量**

在 `.env.example` 的 `LANGFUSE_HOST=` 行（L33）之后追加：

```
# 日志配置
LOG_LEVEL=INFO
LOG_DIR=logs
LOG_MAX_BYTES=67108864
LOG_BACKUP_COUNT=7
```

- [ ] **Step 7: 修改 main.py — 接入 setup_logging + print→logger + flush**

在 `app/main.py` 做以下改动：

1. 文件顶部 import 区（L34 后）追加：
```python
import logging
from app.utils.logging_setup import setup_logging

logger = logging.getLogger(__name__)
```

2. lifespan 函数体最开头（L40 之前，`model_config = load_model_config` 之前）插入：
```python
    setup_logging()
```

3. 将 lifespan 中所有 `print(...)` 替换为 `logger.info(...)`。具体行（当前行号可能因 Step 2 插入而整体下移 1 行）：
   - L77 `print(f"Workspace manager initialized [opensandbox] ...")` → `logger.info("Workspace manager initialized [opensandbox] (domain=%s, image=%s, ttl=%s, pool_size=%s)", OPENSANDBOX_DOMAIN, OPENSANDBOX_IMAGE, WORKSPACE_TTL, OPENSANDBOX_POOL_SIZE)`
   - L90 `print(f"Workspace manager initialized [docker] ...")` → `logger.info("Workspace manager initialized [docker] (image=%s, basedir=%s, ttl=%s)", WORKSPACE_BASE_IMAGE, WORKSPACE_BASEDIR, WORKSPACE_TTL)`
   - L106 `print(f"Workspace cleanup service started ...")` → `logger.info("Workspace cleanup service started (retention=%d天, interval=%d小时)", WORKSPACE_RETENTION_DAYS, WORKSPACE_CLEANUP_INTERVAL_HOURS)`
   - L115 `print("Orchestrator service initialized ...")` → `logger.info("Orchestrator service initialized (multi-agent + multi-intent)")`
   - L123 `print(f"Redis client initialized ...")` → `logger.info("Redis client initialized (%s)", REDIS_URL)`
   - L144-147 `print(f"Session service initialized ...")` → `logger.info("Session service initialized (MySQL: %s@%s:%s/%s)", MYSQL_USER, MYSQL_HOST, MYSQL_PORT, MYSQL_DATABASE)`
   - L152 `print("Langfuse service initialized")` → `logger.info("Langfuse service initialized")`
   - L154 `print("Langfuse service disabled ...")` → `logger.info("Langfuse service disabled (credentials not configured)")`
   - L164 `print("Workspace manager closed")` → `logger.info("Workspace manager closed")`
   - L167 `print("Workspace cleanup service stopped")` → `logger.info("Workspace cleanup service stopped")`
   - L172 `print("MySQL pool closed")` → `logger.info("MySQL pool closed")`
   - L176 `print("Redis connection closed")` → `logger.info("Redis connection closed")`

4. 在 lifespan 的 `yield` 之后（L176 `redis_client.close()` 之后、函数结束之前）追加 P0-4 flush：
```python
    # P0-4: Langfuse 批量缓冲区落盘，避免滚动发布丢失未上报 trace
    try:
        app.state.langfuse_service.flush()
        logger.info("Langfuse service flushed")
    except Exception:
        logger.warning("Langfuse flush on shutdown failed", exc_info=True)
```

- [ ] **Step 8: 修改 docker-compose.yml 增 logs 卷**

在 `docker-compose.yml` 的 cdcc-cia 服务的 `volumes:` 段下追加一行 `- ./logs:/app/logs`（具体缩进与现有 volumes 对齐）。

- [ ] **Step 9: 验证 main.py 可正常导入**

Run: `.venv/bin/python -c "from app.main import app; print('OK')"`
Expected: 输出 OK，无 ImportError

- [ ] **Step 10: 提交检查点**

改动清单：`app/utils/logging_setup.py`（新建）、`app/config.py`（+5 行）、`app/main.py`（setup_logging + print→logger + flush）、`.env.example`（+5 行）、`docker-compose.yml`（+1 行）、`tests/test_logging_setup.py`（新建）

---

### Task 5: P0-6a 扩展 langfuse_service + get_current_langfuse + metrics stub

**Files:**
- Modify: `app/services/langfuse_service.py`（L36-66 start/end_observation 扩展参数；文件末尾追加 get_current_langfuse）
- Create: `app/utils/metrics.py`（stub，P2 替换）
- Test: `tests/test_langfuse_service_extension.py`

**Interfaces:**
- Consumes: `langfuse.Langfuse` SDK 的 `start_observation` / `observation.update` / `observation.end` 方法
- Produces: `LangfuseService.start_observation(name, as_type, input, model, metadata) -> Optional[Any]`、`LangfuseService.end_observation(observation, output, usage_details, level, status_message) -> None`、`get_current_langfuse() -> Optional[LangfuseService]`、`set_current_langfuse(svc) -> None`、`metrics.record_stage(name, duration_ms, success) -> None`（stub）

- [ ] **Step 1: 写失败测试**

`tests/test_langfuse_service_extension.py`：

```python
"""langfuse_service 扩展参数测试。"""
from unittest.mock import MagicMock

from app.services.langfuse_service import LangfuseService


def _make_enabled_service():
    """创建一个 _enabled=True、_client=mock 的实例（跳过真实初始化）。"""
    svc = LangfuseService.__new__(LangfuseService)
    svc._enabled = True
    svc._client = MagicMock()
    return svc


def test_start_observation_passes_model():
    svc = _make_enabled_service()
    svc._client.start_observation.return_value = MagicMock()

    svc.start_observation(name="llm-x", as_type="generation", model="deepseek-v4")
    kwargs = svc._client.start_observation.call_args.kwargs
    assert kwargs["model"] == "deepseek-v4"


def test_start_observation_passes_metadata():
    svc = _make_enabled_service()
    svc._client.start_observation.return_value = MagicMock()

    svc.start_observation(name="tool-x", as_type="tool", metadata={"k": "v"})
    kwargs = svc._client.start_observation.call_args.kwargs
    assert kwargs["metadata"] == {"k": "v"}


def test_end_observation_passes_usage_details():
    svc = _make_enabled_service()
    obs = MagicMock()

    svc.end_observation(obs, output="text", usage_details={"input": 100, "output": 50})
    kwargs = obs.update.call_args.kwargs
    assert kwargs["usage_details"]["input"] == 100
    assert kwargs["usage_details"]["output"] == 50
    obs.end.assert_called_once()


def test_end_observation_passes_level():
    svc = _make_enabled_service()
    obs = MagicMock()

    svc.end_observation(obs, output={"error": "boom"}, level="ERROR",
                        status_message="ValueError")
    kwargs = obs.update.call_args.kwargs
    assert kwargs["level"] == "ERROR"
    assert kwargs["status_message"] == "ValueError"


def test_end_observation_backward_compatible():
    """旧调用方只传 output，不传新参数，不应报错。"""
    svc = _make_enabled_service()
    obs = MagicMock()

    svc.end_observation(obs, output="text")
    obs.update.assert_called_once()
    obs.end.assert_called_once()


def test_start_observation_disabled_returns_none():
    svc = LangfuseService.__new__(LangfuseService)
    svc._enabled = False
    svc._client = None
    assert svc.start_observation(name="x") is None


def test_get_current_langfuse_returns_none_by_default():
    import app.services.langfuse_service as mod
    mod._current_instance = None
    assert mod.get_current_langfuse() is None


def test_set_and_get_current_langfuse():
    import app.services.langfuse_service as mod
    mod._current_instance = None
    svc = _make_enabled_service()
    mod.set_current_langfuse(svc)
    assert mod.get_current_langfuse() is svc
    mod._current_instance = None  # cleanup
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_langfuse_service_extension.py -v`
Expected: FAIL — `start_observation` 不接受 `model` 参数、`end_observation` 不接受 `usage_details` 参数、`get_current_langfuse` 不存在

- [ ] **Step 3: 扩展 langfuse_service.py**

在 `app/services/langfuse_service.py` 中修改 `start_observation`（L36-52）：

```python
    def start_observation(
        self,
        name: str = "chat-response",
        as_type: str = "span",
        input: Any = None,
        model: Optional[str] = None,
        metadata: Optional[dict] = None,
    ) -> Optional[Any]:
        if not self._enabled or not self._client:
            return None
        try:
            kwargs: dict = {"name": name, "as_type": as_type, "input": input}
            if model:
                kwargs["model"] = model
            if metadata:
                kwargs["metadata"] = metadata
            return self._client.start_observation(**kwargs)
        except Exception as e:
            logger.warning("Langfuse start_observation failed: %s", e)
            return None
```

修改 `end_observation`（L54-66）：

```python
    def end_observation(
        self,
        observation,
        output: Any = None,
        usage_details: Optional[dict] = None,
        level: Optional[str] = None,
        status_message: Optional[str] = None,
    ) -> None:
        if not self._enabled or not observation:
            return
        try:
            kwargs: dict = {"output": output}
            if usage_details:
                kwargs["usage_details"] = usage_details
            if level:
                kwargs["level"] = level
            if status_message:
                kwargs["status_message"] = status_message
            observation.update(**kwargs)
            observation.end()
        except Exception as e:
            logger.warning("Langfuse end_observation failed: %s", e)
```

在文件末尾追加模块级单例访问器：

```python

# ---- 模块级单例访问器 ----

_current_instance: Optional["LangfuseService"] = None


def set_current_langfuse(svc: "LangfuseService") -> None:
    """在 main.py lifespan 中注册，供 chat_complete 等不便无法逐层传参的场景使用。"""
    global _current_instance
    _current_instance = svc


def get_current_langfuse() -> Optional["LangfuseService"]:
    """取当前已注册的 LangfuseService 实例，未注册返回 None。"""
    return _current_instance
```

- [ ] **Step 4: 创建 metrics stub**

`app/utils/metrics.py`：

```python
"""指标 stub：P0 阶段空实现，P2 替换为真实 Prometheus 指标。

所有方法均不抛异常，确保 traced_stage 的 try/except 不会因缺实现而告警。
"""


def record_stage(name: str, duration_ms: int, success: bool) -> None:
    """记录一个环节的耗时与成败。P0 阶段空实现。"""
    pass
```

- [ ] **Step 5: 运行测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_langfuse_service_extension.py -v`
Expected: 8 passed

- [ ] **Step 6: 在 main.py 注册单例**

在 `app/main.py` 的 lifespan 中，`app.state.langfuse_service = LangfuseService()` 之后（L150 之后）追加：

```python
    from app.services.langfuse_service import set_current_langfuse
    set_current_langfuse(app.state.langfuse_service)
```

- [ ] **Step 7: 验证导入链**

Run: `.venv/bin/python -c "from app.main import app; print('OK')"`
Expected: OK

- [ ] **Step 8: 提交检查点**

改动清单：`app/services/langfuse_service.py`（扩展 start/end_observation + 新增 get/set_current_langfuse）、`app/utils/metrics.py`（新建 stub）、`app/main.py`（注册单例）、`tests/test_langfuse_service_extension.py`（新建）

---

### Task 6: P0-6b traced.py 包装器

**Files:**
- Create: `app/utils/traced.py`
- Test: `tests/test_traced.py`

**Interfaces:**
- Consumes: `LangfuseService.start_observation` / `end_observation`（Task 5 扩展版）、`app.utils.metrics.record_stage`
- Produces: `StageRecorder`（句柄类，含 `set_output(**fields)` / `mark_degraded(reason, detail)` / `.success` 属性）、`traced_stage(langfuse_service, name, input, as_type) -> ContextManager[StageRecorder]`

- [ ] **Step 1: 写失败测试**

`tests/test_traced.py`：

```python
"""traced_stage 包装器单元测试。"""
from unittest.mock import MagicMock

import pytest

from app.utils.traced import traced_stage, StageRecorder


def test_traced_stage_records_duration_and_success():
    lf = MagicMock()
    lf.enabled = True
    obs = MagicMock()
    lf.start_observation.return_value = obs

    with traced_stage(lf, "test-stage") as rec:
        rec.set_output(result="ok")

    lf.start_observation.assert_called_once()
    lf.end_observation.assert_called_once()
    end_kwargs = lf.end_observation.call_args.kwargs
    assert end_kwargs["output"]["stage"] == "test-stage"
    assert end_kwargs["output"]["success"] is True
    assert "durationMs" in end_kwargs["output"]
    assert end_kwargs["output"]["result"] == "ok"


def test_traced_stage_records_exception():
    lf = MagicMock()
    lf.enabled = True
    obs = MagicMock()
    lf.start_observation.return_value = obs

    with pytest.raises(ValueError, match="boom"):
        with traced_stage(lf, "test-stage"):
            raise ValueError("boom")

    end_kwargs = lf.end_observation.call_args.kwargs
    assert end_kwargs["output"]["success"] is False
    assert "errorClass" in end_kwargs["output"]
    assert "errorMessage" in end_kwargs["output"]


def test_traced_stage_works_without_langfuse():
    """langfuse_service 为 None 时退化为纯日志，不报错。"""
    with traced_stage(None, "test-stage") as rec:
        rec.set_output(x=1)
    # 无 crash 即通过


def test_traced_stage_mark_degraded():
    lf = MagicMock()
    lf.enabled = True
    obs = MagicMock()
    lf.start_observation.return_value = obs

    with traced_stage(lf, "test-stage") as rec:
        rec.mark_degraded(reason="TimeoutError", detail="LLM timeout")

    end_kwargs = lf.end_observation.call_args.kwargs
    assert end_kwargs["output"]["success"] is False
    assert end_kwargs["output"]["degraded"] is True
    assert end_kwargs["output"]["reason"] == "TimeoutError"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_traced.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 实现 traced.py**

`app/utils/traced.py`（代码取自优化方案 P0-6，见方案文档 L418-485）：

```python
"""统一埋点入口：一次 with 同时产出 Langfuse observation、结构化日志与指标。"""
import logging
import time
from contextlib import contextmanager
from typing import Any, Iterator, Optional

logger = logging.getLogger(__name__)


class StageRecorder:
    """埋点句柄：业务代码通过它写 output 与扩展字段。"""

    def __init__(self, observation: Any) -> None:
        self._obs = observation
        self._extra: dict = {}
        self.success = True

    def set_output(self, **fields: Any) -> None:
        self._extra.update(fields)

    def mark_degraded(self, reason: str, detail: str = "") -> None:
        """记录降级：降级率与降级原因分布是意图链路的关键指标。"""
        self.success = False
        self._extra.update({"degraded": True, "reason": reason, "detail": detail[:500]})


@contextmanager
def traced_stage(
    langfuse_service: Optional[Any],
    name: str,
    input: Any = None,
    as_type: str = "span",
) -> Iterator[StageRecorder]:
    """环节埋点。退出时自动写 duration_ms/success，异常自动归因并重抛。

    观测失败绝不影响业务：langfuse 未启用时退化为纯日志+指标。
    """
    started = time.perf_counter()
    obs = None
    if langfuse_service is not None:
        obs = langfuse_service.start_observation(name=name, as_type=as_type, input=input)
    recorder = StageRecorder(obs)
    try:
        yield recorder
    except Exception as e:
        recorder.success = False
        recorder._extra.update({
            "errorClass": f"{type(e).__module__}.{type(e).__name__}",
            "errorMessage": str(e)[:500],
        })
        raise
    finally:
        duration_ms = int((time.perf_counter() - started) * 1000)
        payload = {"stage": name, "durationMs": duration_ms,
                   "success": recorder.success, **recorder._extra}
        try:
            if langfuse_service is not None and obs is not None:
                langfuse_service.end_observation(obs, output=payload)
        except Exception:
            logger.debug("[traced_stage] observation 收尾失败", exc_info=True)
        logger.info("[stage] %s %dms", name, duration_ms, extra={"biz": payload})
        try:
            from app.utils.metrics import record_stage
            record_stage(name, duration_ms, recorder.success)
        except Exception:
            pass
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_traced.py -v`
Expected: 4 passed

- [ ] **Step 5: 提交检查点**

改动清单：`app/utils/traced.py`（新建）、`tests/test_traced.py`（新建）

---

### Task 7: P0-3a 后端 trace_id 流首下发

**Files:**
- Modify: `app/services/chat_service.py`（L419-421 插入早期 trace_ready；L265-288 _finalize_trace 增 already_emitted 参数；L515 调用点传参）
- Test: `tests/test_trace_ready.py`

**Interfaces:**
- Consumes: `LangfuseService.start_span`（现有）、`root_obs.trace_id`（T4 实测：span 内可读）
- Produces: `_finalize_trace(..., already_emitted: bool = False)` 的新签名

- [ ] **Step 1: 写失败测试**

`tests/test_trace_ready.py`：

```python
"""trace_ready 早期下发 + _finalize_trace already_emitted 测试。"""
import json

import pytest
from unittest.mock import MagicMock

from app.services.chat_service import _finalize_trace


@pytest.mark.asyncio
async def test_finalize_trace_skips_when_already_emitted():
    """已提前下发过 trace_ready 时，_finalize_trace 不重复下发。"""
    lf = MagicMock()
    lf.enabled = True
    lf.flush = MagicMock()

    events = []
    async for ev in _finalize_trace(lf, None, None, "sess-1",
                                    already_emitted=True):
        events.append(ev)

    assert len(events) == 0
    lf.flush.assert_called_once()


@pytest.mark.asyncio
async def test_finalize_trace_emits_when_not_emitted():
    """未提前下发时，_finalize_trace 仍按原逻辑下发。"""
    lf = MagicMock()
    lf.enabled = True
    lf.flush = MagicMock()
    root_obs = MagicMock()
    root_obs.trace_id = "abc123def456"

    events = []
    async for ev in _finalize_trace(lf, root_obs, None, "sess-1",
                                    already_emitted=False):
        events.append(ev)

    assert len(events) == 1
    data = json.loads(events[0][6:].strip())
    assert data["type"] == "trace_ready"
    assert data["trace_id"] == "abc123def456"


@pytest.mark.asyncio
async def test_finalize_trace_saves_trace_id():
    """flush 后仍保存 trace_id 到 session。"""
    lf = MagicMock()
    lf.enabled = True
    lf.flush = MagicMock()
    root_obs = MagicMock()
    root_obs.trace_id = "trace-xyz"
    session_service = MagicMock()

    events = []
    async for ev in _finalize_trace(lf, root_obs, session_service, "sess-1",
                                    already_emitted=True):
        events.append(ev)

    # 不重复下发，但 flush 仍执行
    lf.flush.assert_called_once()
    # already_emitted=True 时 trace_id 为 None（不在 finalize 阶段读取），不保存
    session_service.save_latest_trace_id.assert_not_called()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_trace_ready.py -v`
Expected: FAIL — `_finalize_trace` 不接受 `already_emitted` 参数

- [ ] **Step 3: 修改 _finalize_trace 增 already_emitted 参数**

将 `app/services/chat_service.py` 的 `_finalize_trace`（L265-288）替换为：

```python
async def _finalize_trace(
    langfuse_service: LangfuseService,
    root_obs: Any,
    session_service: Any,
    session_id: Optional[str],
    already_emitted: bool = False,
) -> AsyncGenerator[str, None]:
    """Langfuse trace 收尾：flush + （可选）yield trace_ready + 保存 trace_id。

    在根 span context manager 之外执行，保证 span 已 end。
    already_emitted=True 时跳过 trace_ready 下发（已在流首下发过），
    但仍执行 flush 与 save_latest_trace_id。
    """
    trace_id = None
    if langfuse_service and langfuse_service.enabled:
        try:
            langfuse_service.flush()
            trace_id = root_obs.trace_id if root_obs else None
        except Exception:
            pass

    if not already_emitted:
        trace_event = json.dumps({"type": "trace_ready", "trace_id": trace_id})
        yield f"data: {trace_event}\n\n"

    # 保存 trace_id
    if session_service and session_id and trace_id:
        await session_service.save_latest_trace_id(session_id, trace_id)
```

- [ ] **Step 4: 在 generate_response 中插入早期 trace_ready**

在 `app/services/chat_service.py` 的 `generate_response` 函数中：

1. 在 `root_obs = None`（L419）之后、`try:`（L420）之前，插入：
```python
    trace_emitted = False
```

2. 在 `with root_span_ctx as root_obs:`（L421）之后、`# ④ 执行编排主流程`（L422）之前，插入：
```python
            # P0-3: 流首即下发真实 trace_id（T4 实测：span 内即可读）
            trace_id = getattr(root_obs, "trace_id", None) if root_obs else None
            if trace_id:
                trace_emitted = True
                yield f"data: {json.dumps({'type': 'trace_ready', 'trace_id': trace_id})}\n\n"
```

3. 在 `_finalize_trace` 调用处（L515），传入 `already_emitted`：
```python
        async for ev in _finalize_trace(
            langfuse_service, root_obs, session_service, session_id,
            already_emitted=trace_emitted,
        ):
            yield ev
```

- [ ] **Step 5: 运行测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_trace_ready.py -v`
Expected: 3 passed

- [ ] **Step 6: 全量回归**

Run: `.venv/bin/python -m pytest tests/ -v`
Expected: 所有测试通过（trace_context 5 + trace_names 5 + logging_setup 4 + langfuse_service 8 + traced 4 + trace_ready 3 = 29 passed）

- [ ] **Step 7: 提交检查点**

改动清单：`app/services/chat_service.py`（早期 trace_ready + _finalize_trace 增参）、`tests/test_trace_ready.py`（新建）

---

### Task 8: P0-3b 前端 TRACE_READY 事件类型 + normalizer

**Files:**
- Modify: `src/types/ai-stream.ts`（L69 前增 `"TRACE_READY"`；L246 前增 TraceReadyEvent 接口 + 联合类型成员）
- Modify: `src/services/ai-stream/event-parser.ts`（eventNormalizerRegistry 增 `trace_ready` normalizer）
- Test: `src/services/ai-stream/event-parser.test.ts`（新增测试用例）

**Interfaces:**
- Consumes: `RawSseEvent`（现有类型，含 `type` / `trace_id` 字段）
- Produces: `TraceReadyEvent`（新接口，`type: "TRACE_READY"`、`traceId: string`）

- [ ] **Step 1: 写失败测试**

在 `src/services/ai-stream/event-parser.test.ts` 末尾（L59 `});` 之前）追加：

```typescript

  it("normalizes trace_ready event with traceId", () => {
    const normalized = normalizeRawSseEvent(
      { id: "tr1", type: "trace_ready", trace_id: "a4cb53c2949e9e69d4a51b2ffdc0a265" },
      { sessionId: "session-1" },
    );
    expect(normalized).toHaveLength(1);
    expect(normalized[0]?.type).toBe("TRACE_READY");
    expect(
      normalized[0] && "traceId" in normalized[0] ? normalized[0].traceId : "",
    ).toBe("a4cb53c2949e9e69d4a51b2ffdc0a265");
  });
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ../goodChatAgentscope && npx vitest run src/services/ai-stream/event-parser.test.ts`
Expected: FAIL — `trace_ready` 未注册为 normalizer，事件落入 UNKNOWN

- [ ] **Step 3: 在 ai-stream.ts 增类型定义**

在 `src/types/ai-stream.ts` 中：

1. 在 L68 `"RECOMMENDED_QUESTIONS"` 之后、L69 `"UNKNOWN"` 之前，插入：
```typescript
  | "TRACE_READY"
```

2. 在 L221 `export interface UnknownStreamEvent` 之前，插入：
```typescript
export interface TraceReadyEvent extends AiStreamEventBase {
  type: "TRACE_READY";
  traceId: string;
}

```

3. 在 L246 `| RecommendedQuestionsEvent` 之后、L247 `| UnknownStreamEvent` 之前，插入：
```typescript
  | TraceReadyEvent
```

- [ ] **Step 4: 在 event-parser.ts 增 normalizer**

在 `src/services/ai-stream/event-parser.ts` 的 `eventNormalizerRegistry` 对象中，在 `recommended_questions` normalizer（L271-287）之后、闭合 `}` 之前，追加：

```typescript
  trace_ready: (raw) => [
    {
      ...buildBaseEvent(raw, "TRACE_READY", normalizeReplyId(raw)),
      traceId: typeof raw.trace_id === "string" ? raw.trace_id : "",
    },
  ],
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd ../goodChatAgentscope && npx vitest run src/services/ai-stream/event-parser.test.ts`
Expected: 3 passed（原有 2 个 + 新增 1 个）

- [ ] **Step 6: 提交检查点**

改动清单：`src/types/ai-stream.ts`（+TRACE_READY 类型）、`src/services/ai-stream/event-parser.ts`（+trace_ready normalizer）、`src/services/ai-stream/event-parser.test.ts`（+测试用例）

---

### Task 9: P0-3c 前端移除 createTraceId 兜底 + TRACE_READY 消费

**Files:**
- Modify: `src/hooks/use-chat-stream.ts`（L91 删 `traceId: createTraceId()`；handleNormalizedEvent 增 TRACE_READY 处理）
- Modify: `src/services/sessions-api.ts`（L89 删 `createTraceId()` 兜底）

**Interfaces:**
- Consumes: `TraceReadyEvent`（Task 8 产出）、`updateAssistantById`（现有工具函数）
- Produces: assistant 占位消息不再自带假 traceId；收到 TRACE_READY 事件后写入真实 traceId

- [ ] **Step 1: 修改 use-chat-stream.ts — 删除假 traceId**

在 `src/hooks/use-chat-stream.ts` 中，将 L91 的：
```typescript
        traceId: createTraceId(),
```
删除（占位消息不再有 `traceId` 字段，初始为 `undefined`）。

同时删除文件顶部 `createTraceId` 的 import（L5），如果该 import 不再被文件内其他代码使用。用 grep 确认：
```bash
grep -n "createTraceId" src/hooks/use-chat-stream.ts
```
如果仅 L5 import 和 L91 使用两处，则整行删除 import。如果 `createTraceId` 在文件内还有其他用途，则保留 import。

- [ ] **Step 2: 修改 use-chat-stream.ts — 增 TRACE_READY 事件处理**

在 `handleNormalizedEvent` 函数体中（L150-174），在 `// SSE 返回真实 sessionId 时执行会话 ID 迁移`（L159）之前，插入：

```typescript
        // 后端下发真实 trace_id，写入对应 assistant 消息供反馈上报使用
        if (event.type === "TRACE_READY" && event.traceId) {
          const state = useChatStore.getState();
          const messages = state.conversationMessages[ctx.sessionId] ?? [];
          state.setConversationMessages(
            ctx.sessionId,
            updateAssistantById(messages, assistantMessageId, (msg) => ({
              ...msg,
              traceId: event.traceId,
            })),
          );
        }
```

确认 `updateAssistantById` 已在文件中 import 或定义。用 grep 检查：
```bash
grep -n "updateAssistantById" src/hooks/use-chat-stream.ts
```
如果未 import，在文件顶部 import 区追加：
```typescript
import { updateAssistantById } from "@/hooks/use-chat-actions";
```
（该函数在 `use-chat-actions.ts` 中定义，需先 grep 确认导出位置）

- [ ] **Step 3: 修改 sessions-api.ts — 删除兜底**

在 `src/services/sessions-api.ts` 中，将 L89 的：
```typescript
        traceId: message.trace_id || (role === "assistant" ? createTraceId() : undefined),
```
改为：
```typescript
        traceId: message.trace_id || undefined,
```

同时删除文件顶部 `createTraceId` 的 import（L3），如果该 import 不再被文件内其他代码使用。用 grep 确认：
```bash
grep -n "createTraceId" src/services/sessions-api.ts
```

- [ ] **Step 4: 验证前端编译通过**

Run: `cd ../goodChatAgentscope && npx tsc --noEmit`
Expected: 无 TypeScript 编译错误

- [ ] **Step 5: 验证前端测试通过**

Run: `cd ../goodChatAgentscope && npx vitest run`
Expected: 所有测试通过

- [ ] **Step 6: 验证 lint 通过**

Run: `cd ../goodChatAgentscope && npm run lint`
Expected: 无 lint 错误

- [ ] **Step 7: 提交检查点**

改动清单：`src/hooks/use-chat-stream.ts`（删假 traceId + 增 TRACE_READY 消费）、`src/services/sessions-api.ts`（删兜底）

---

## Self-Review

### 1. Spec coverage

| 优化方案条目 | 对应 Task | 状态 |
|---|---|---|
| P0-1 trace 上下文模块 | Task 2 | ✅ |
| P0-2 日志初始化 + trace_id 自动注入 | Task 4 | ✅ |
| P0-3 trace_id 全链路对齐（后端） | Task 7 | ✅ |
| P0-3 trace_id 全链路对齐（前端类型+normalizer） | Task 8 | ✅ |
| P0-3 trace_id 全链路对齐（前端消费+删兜底） | Task 9 | ✅ |
| P0-4 关停时 flush | Task 4 Step 7 | ✅（折入 main.py 改动） |
| P0-5 命名枚举 + 版本收紧 | Task 1 + Task 3 | ✅ |
| P0-6 统一埋点包装器（含 langfuse_service 扩展） | Task 5 + Task 6 | ✅ |

无遗漏。

### 2. Placeholder scan

- 无 "TBD" / "TODO" / "fill in" — ✅
- 无 "add appropriate error handling" — 所有 try/except 都写了具体代码 — ✅
- 无 "similar to Task N" — ✅
- 所有 code step 都有实际代码 — ✅

### 3. Type consistency

| 符号 | 定义位置 | 使用位置 | 一致性 |
|---|---|---|---|
| `set_request_context(session_id, user_id)` | Task 2 | Task 4 (logging_setup import) | ✅ |
| `get_trace_id() -> Optional[str]` | Task 2 | Task 4 (logging_setup import) | ✅ |
| `TraceName` 枚举 | Task 3 | (P1 将使用) | ✅ |
| `LangfuseService.start_observation(..., model, metadata)` | Task 5 | Task 6 (traced.py calls it) | ✅ |
| `LangfuseService.end_observation(..., usage_details, level, status_message)` | Task 5 | Task 6 (traced.py calls it) | ✅ |
| `get_current_langfuse() -> Optional[LangfuseService]` | Task 5 | (P1-1 will use) | ✅ |
| `set_current_langfuse(svc)` | Task 5 | Task 4 Step 7 (main.py calls it) → 实际在 Task 5 Step 6 | ✅ |
| `StageRecorder.set_output(**fields)` | Task 6 | (P1 will use) | ✅ |
| `StageRecorder.mark_degraded(reason, detail)` | Task 6 | (P1-4 will use) | ✅ |
| `traced_stage(lf, name, input, as_type)` | Task 6 | (P1-3 will use) | ✅ |
| `_finalize_trace(..., already_emitted)` | Task 7 | Task 7 Step 4 (chat_service 调用点) | ✅ |
| `TraceReadyEvent` | Task 8 | Task 9 (use-chat-stream 消费) | ✅ |
| `record_stage(name, duration_ms, success)` | Task 5 (metrics stub) | Task 6 (traced.py import) | ✅ |

全部一致。

---

*Plan complete and saved to `docs/research/2026-08-11-链路追踪埋点优化-P0实施计划.md`.*

**Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using ch-executing-plans, batch execution with checkpoints

**Which approach?**
