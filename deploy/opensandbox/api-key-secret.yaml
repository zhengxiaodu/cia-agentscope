apiVersion: v1
kind: Secret
metadata:
  name: opensandbox-api-key
  namespace: opensandbox-system
type: Opaque
stringData:
  # 生产环境请使用随机生成的 64 位 hex 字符串
  # 生成方式: openssl rand -hex 32
  api-key: "YOUR_64_CHAR_HEX_API_KEY_HERE"
